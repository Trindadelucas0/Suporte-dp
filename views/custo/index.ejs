<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: { red: '#DC2626', yellow: '#FBBF24' }
                    }
                }
            }
        }
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bungee+Inline&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .bungee-inline-regular {
            font-family: "Bungee Inline", sans-serif;
            font-weight: 400;
            font-style: normal;
        }
    </style>
</head>
<body class="bg-gray-50">
    <% if (typeof user !== 'undefined' && user) { %>
        <%- include('../partials/navbar') %>
    <% } %>

    <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
        <div class="mb-6">
            <h1 class="text-3xl font-bold text-gray-900">Simulador de Custo do Funcionário</h1>
            <p class="mt-2 text-gray-600">Cálculo completo de todos os custos trabalhistas</p>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
                <div class="bg-white rounded-xl shadow-xl p-4 sm:p-6 border-4 border-primary-red hover:border-primary-yellow transition-all duration-300 transform hover:shadow-2xl">
                    <h2 class="text-xl sm:text-2xl font-bold mb-4 sm:mb-6 bg-gradient-to-r from-primary-red to-primary-yellow bg-clip-text text-transparent">
                        <i class="fas fa-calculator text-primary-red mr-2"></i>Dados de Entrada
                    </h2>
                    
                    <% if (error) { %>
                        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded mb-4">
                            <%= error %>
                        </div>
                    <% } %>

                    <form method="POST" action="/custo/calcular" class="space-y-4">
                        <% if (typeof csrfToken !== 'undefined' && csrfToken) { %>
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <% } %>
                        <div>
                            <label class="block text-sm font-medium mb-2">Salário Bruto (R$)</label>
                            <input type="number" name="salarioBruto" step="0.01" required
                                class="w-full px-4 py-2 border rounded-md">
                        </div>

                        <div>
                            <label class="block text-sm font-medium mb-2">Tipo de Registro</label>
                            <select name="tipoRegistro" class="w-full px-4 py-2 border rounded-md">
                                <option value="clt_geral">CLT Geral</option>
                                <option value="jovem_aprendiz">Jovem Aprendiz</option>
                                <option value="domestico">Doméstico</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-sm font-medium mb-2">Regime Tributário</label>
                            <select name="regimeTributario" class="w-full px-4 py-2 border rounded-md">
                                <option value="simples_nacional">Simples Nacional</option>
                                <option value="lucro_presumido">Lucro Presumido</option>
                                <option value="lucro_real">Lucro Real</option>
                            </select>
                            <p class="text-xs text-gray-500 mt-1">Para cálculo de impostos sobre folha</p>
                        </div>

                        <div>
                            <label class="block text-sm font-medium mb-2">Benefícios (R$)</label>
                            <input type="number" name="beneficios" step="0.01" value="0"
                                class="w-full px-4 py-2 border rounded-md"
                                placeholder="VT, VR, Plano de Saúde, etc.">
                        </div>

                        <div>
                            <label class="block text-sm font-medium mb-2">Encargos Adicionais (R$)</label>
                            <input type="number" name="encargosAdicionais" step="0.01" value="0"
                                class="w-full px-4 py-2 border rounded-md">
                        </div>

                        <button type="submit" class="w-full bg-gradient-to-r from-primary-red to-primary-yellow text-white py-3 rounded-md font-semibold hover:opacity-90">
                            Calcular Custo Total
                        </button>
                    </form>
                </div>
            </div>

            <div class="lg:col-span-2">
                <% if (resultado) { %>
                    <div class="bg-white rounded-xl shadow-xl p-4 sm:p-6 border-4 border-primary-red hover:border-primary-yellow transition-all duration-300 transform hover:shadow-2xl">
                        <div class="bg-gradient-to-r from-primary-red to-primary-yellow rounded-lg p-6 text-white mb-6">
                            <h3 class="text-2xl font-bold mb-4">Custo Total do Funcionário</h3>
                            <div class="grid grid-cols-2 gap-4">
                                <div>
                                    <p class="text-sm opacity-90">Custo Mensal</p>
                                    <p class="text-3xl font-bold">R$ <%= resultado.custoMensal.toFixed(2) %></p>
                                </div>
                                <div>
                                    <p class="text-sm opacity-90">Custo Anual</p>
                                    <p class="text-3xl font-bold">R$ <%= resultado.custoAnual.toFixed(2) %></p>
                                </div>
                            </div>
                        </div>

                        <h4 class="text-lg font-bold mb-4">Composição do Custo Mensal</h4>
                        <div class="space-y-3 mb-6">
                            <div class="flex justify-between border-b pb-2">
                                <span>Salário Bruto</span>
                                <span class="font-semibold">R$ <%= resultado.salarioBruto.toFixed(2) %></span>
                            </div>
                            <% if (resultado.inssPatronal) { %>
                                <div class="flex justify-between border-b pb-2">
                                    <span>INSS Patronal (20%)</span>
                                    <span class="font-semibold">R$ <%= resultado.inssPatronal.toFixed(2) %></span>
                                </div>
                            <% } %>
                            <% if (resultado.impostosRegime) { %>
                                <div class="flex justify-between border-b pb-2">
                                    <span>Impostos - <%= resultado.regimeTributario.replace('_', ' ').toUpperCase() %></span>
                                    <span class="font-semibold">R$ <%= resultado.impostosRegime.toFixed(2) %></span>
                                </div>
                            <% } %>
                            <div class="flex justify-between border-b pb-2">
                                <span>Férias Proporcionais (1/12 + 1/3)</span>
                                <span class="font-semibold">R$ <%= resultado.valorFeriasMensal.toFixed(2) %></span>
                            </div>
                            <div class="flex justify-between border-b pb-2">
                                <span>13º Salário (1/12)</span>
                                <span class="font-semibold">R$ <%= resultado.valorDecimoTerceiroMensal.toFixed(2) %></span>
                            </div>
                            <div class="flex justify-between border-b pb-2">
                                <span>FGTS</span>
                                <span class="font-semibold">R$ <%= resultado.valorFGTS.toFixed(2) %></span>
                            </div>
                            <% if (resultado.beneficios > 0) { %>
                                <div class="flex justify-between border-b pb-2">
                                    <span>Benefícios</span>
                                    <span class="font-semibold">R$ <%= resultado.beneficios.toFixed(2) %></span>
                                </div>
                            <% } %>
                        </div>

                        <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4">
                            <h5 class="font-bold mb-2">Memória de Cálculo</h5>
                            <div class="space-y-2 text-sm">
                                <% resultado.memoria.forEach(passo => { %>
                                    <div class="<%= passo.destaque ? 'font-bold text-primary-red' : '' %>">
                                        <%= passo.descricao %>: R$ <%= passo.valor %>
                                    </div>
                                <% }); %>
                            </div>
                        </div>
                    </div>
                <% } else { %>
                    <div class="bg-white rounded-lg shadow-md p-12 text-center">
                        <i class="fas fa-calculator text-gray-300 text-6xl mb-4"></i>
                        <p class="text-gray-600">Preencha os dados para calcular o custo total</p>
                    </div>
                <% } %>
            </div>
        </div>
    </main>
</body>
</html>

