<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: { red: '#DC2626', yellow: '#FBBF24' }
                    }
                }
            }
        }
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bungee+Inline&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Favicon com as letras "DP" nas cores do sistema (Vermelho #DC2626 e Amarelo #FBBF24) -->
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 200 200'%3E%3Cdefs%3E%3ClinearGradient id='grad' x1='0%25' y1='0%25' x2='100%25' y2='100%25'%3E%3Cstop offset='0%25' style='stop-color:%23DC2626;stop-opacity:1' /%3E%3Cstop offset='100%25' style='stop-color:%23FBBF24;stop-opacity:1' /%3E%3C/linearGradient%3E%3C/defs%3E%3Crect width='200' height='200' rx='20' fill='url(%23grad)'/%3E%3Ctext x='100' y='140' font-family='Arial, sans-serif' font-size='120' font-weight='bold' text-anchor='middle' fill='white'%3EDP%3C/text%3E%3C/svg%3E" />
    <link rel="apple-touch-icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 200 200'%3E%3Cdefs%3E%3ClinearGradient id='grad' x1='0%25' y1='0%25' x2='100%25' y2='100%25'%3E%3Cstop offset='0%25' style='stop-color:%23DC2626;stop-opacity:1' /%3E%3Cstop offset='100%25' style='stop-color:%23FBBF24;stop-opacity:1' /%3E%3C/linearGradient%3E%3C/defs%3E%3Crect width='200' height='200' rx='20' fill='url(%23grad)'/%3E%3Ctext x='100' y='140' font-family='Arial, sans-serif' font-size='120' font-weight='bold' text-anchor='middle' fill='white'%3EDP%3C/text%3E%3C/svg%3E" />
    <style>
        .bungee-inline-regular {
            font-family: "Bungee Inline", sans-serif;
            font-weight: 400;
            font-style: normal;
        }
    </style>
</head>

<body class="bg-gray-50">
    <% if (typeof user !=='undefined' && user) { %>
        <%- include('../partials/navbar') %>
            <% } %>

                <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
                    <div class="mb-6">
                        <h1 class="text-3xl font-bold text-gray-900">Periculosidade e Insalubridade</h1>
                        <p class="mt-2 text-gray-600">Adicionais de risco</p>
                    </div>

                    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                        <div class="lg:col-span-1">
                            <div class="bg-white rounded-xl shadow-xl p-4 sm:p-6 border-4 border-primary-red hover:border-primary-yellow transition-all duration-300 transform hover:shadow-2xl">
                                <h2 class="text-xl sm:text-2xl font-bold mb-4 sm:mb-6 bg-gradient-to-r from-primary-red to-primary-yellow bg-clip-text text-transparent">
                                    <i class="fas fa-calculator text-primary-red mr-2"></i>Dados de Entrada
                                </h2>

                                <% if (error) { %>
                                    <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded mb-4">
                                        <%= error %>
                                    </div>
                                    <% } %>

                                        <form method="POST" action="/periculosidade/calcular" class="space-y-4">
                                            <% if (typeof csrfToken !=='undefined' && csrfToken) { %>
                                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                                <% } %>
                                                    <div>
                                                        <label class="block text-sm font-medium mb-2">Ano *</label>
                                                        <select name="ano" id="ano" required
                                                            class="w-full px-4 py-2 border rounded-md">
                                                            <option value="">Selecione o ano</option>
                                                            <option value="2024">2024</option>
                                                            <option value="2025">2025</option>
                                                            <option value="2026">2026</option>
                                                        </select>
                                                        <p class="text-xs text-gray-500 mt-1">Ano para usar o salário
                                                            mínimo correto</p>
                                                    </div>

                                                    <div>
                                                        <label class="block text-sm font-medium mb-2">Tipo *</label>
                                                        <select name="tipo" id="tipo" required
                                                            class="w-full px-4 py-2 border rounded-md">
                                                            <option value="">Selecione o tipo</option>
                                                            <option value="periculosidade">Periculosidade (30% sobre
                                                                salário mínimo)</option>
                                                            <option value="insalubridade">Insalubridade (% sobre salário
                                                                mínimo)</option>
                                                        </select>
                                                    </div>

                                                    <div id="divGrau" class="hidden">
                                                        <label class="block text-sm font-medium mb-2">Grau de
                                                            Insalubridade *</label>
                                                        <select name="grauInsalubridade"
                                                            class="w-full px-4 py-2 border rounded-md">
                                                            <option value="minimo">Mínimo (10%)</option>
                                                            <option value="medio" selected>Médio (20%)</option>
                                                            <option value="maximo">Máximo (40%)</option>
                                                        </select>
                                                    </div>

                                                    <div class="bg-blue-50 border-l-4 border-blue-400 p-4 rounded">
                                                        <p class="text-sm text-blue-800">
                                                            <i class="fas fa-info-circle mr-2"></i>
                                                            <strong>Importante:</strong> A
                                                            <strong>periculosidade</strong> é calculada sobre o
                                                            <strong>salário contratual do funcionário</strong>, enquanto
                                                            a <strong>insalubridade</strong> é calculada sobre o
                                                            <strong>salário mínimo nacional</strong>.
                                                        </p>
                                                    </div>


                                                    <button type="submit"
                                                        class="w-full bg-primary-red text-white py-3 rounded-md font-semibold hover:opacity-90">
                                                        Calcular
                                                    </button>
                                        </form>
                            </div>
                        </div>

                        <div class="lg:col-span-2">
                            <% if (resultado) { %>
                                <div class="bg-white rounded-xl shadow-xl p-4 sm:p-6 border-4 border-primary-red hover:border-primary-yellow transition-all duration-300 transform hover:shadow-2xl">
                                    <div
                                        class="bg-gradient-to-r from-primary-red to-primary-yellow rounded-lg p-6 text-white mb-6">
                                        <h3 class="text-2xl font-bold mb-4">
                                            <%= resultado.tipo==='periculosidade' ? 'Periculosidade' : 'Insalubridade'
                                                %>
                                        </h3>
                                        <p class="text-3xl font-bold">R$ <%= resultado.valorAdicional.toFixed(2) %>
                                        </p>
                                        <p class="text-sm opacity-90 mt-2">
                                            <%= resultado.percentual %>% sobre salário mínimo <%= resultado.ano %>
                                        </p>
                                        <p class="text-xs opacity-75 mt-1">Salário Mínimo: R$ <%=
                                                resultado.salarioMinimo.toFixed(2) %>
                                        </p>
                                    </div>

                                    <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-6">
                                        <h4 class="font-bold mb-2">Educação</h4>
                                        <p class="text-sm text-yellow-800">
                                            <%= resultado.educacao.descricao %>
                                        </p>
                                    </div>

                                    <h4 class="font-bold mb-4">Memória de Cálculo</h4>
                                    <div class="space-y-3">
                                        <% resultado.memoria.forEach(passo=> { %>
                                            <div class="bg-gray-50 rounded-lg p-4 border-l-4 border-primary-red">
                                                <p class="font-semibold">
                                                    <%= passo.descricao %>
                                                </p>
                                                <% if (passo.calculo) { %>
                                                    <p class="text-sm text-gray-600 mt-1">
                                                        <%= passo.calculo %>
                                                    </p>
                                                    <% } %>
                                            </div>
                                            <% }); %>
                                    </div>

                                    <div class="mt-6 bg-gray-50 rounded-lg p-6">
                                        <h4 class="font-bold mb-2">Base Legal</h4>
                                        <p class="font-semibold">
                                            <%= resultado.baseLegal.titulo %>
                                        </p>
                                        <p class="text-sm text-gray-600 mt-1">
                                            <%= resultado.baseLegal.descricao %>
                                        </p>
                                    </div>
                                </div>
                                <% } else { %>
                                    <div class="bg-white rounded-lg shadow-md p-12 text-center">
                                        <i class="fas fa-shield-alt text-gray-300 text-6xl mb-4"></i>
                                        <p class="text-gray-600">Preencha os dados para calcular</p>
                                    </div>
                                    <% } %>
                        </div>
                    </div>
                </main>

                <script>
                    document.getElementById('tipo').addEventListener('change', function () {
                        const divGrau = document.getElementById('divGrau');

                        if (this.value === 'insalubridade') {
                            divGrau.classList.remove('hidden');
                        } else {
                            divGrau.classList.add('hidden');
                        }
                    });
                </script>
</body>

</html>