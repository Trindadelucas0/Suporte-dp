<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: { red: '#DC2626', yellow: '#FBBF24' }
                    }
                }
            }
        }
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bungee+Inline&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .bungee-inline-regular {
            font-family: "Bungee Inline", sans-serif;
            font-weight: 400;
            font-style: normal;
        }
    </style>
</head>
<body class="bg-gray-50">
    <% if (typeof user !== 'undefined' && user) { %>
        <%- include('../partials/navbar') %>
    <% } %>

    <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
        <div class="mb-6">
            <h1 class="text-3xl font-bold text-gray-900 mb-2">
                <i class="fas fa-file-contract text-primary-yellow mr-2"></i>
                Multa de Quebra de Contrato de Experiência
            </h1>
            <div class="bg-blue-50 border-l-4 border-blue-400 p-4 rounded">
                <p class="text-gray-700 mb-2">
                    <i class="fas fa-info-circle text-blue-500 mr-2"></i>
                    <strong>O que é a Multa de Quebra?</strong>
                </p>
                <p class="text-sm text-gray-600 mb-2">
                    Contrato de experiência pode durar até 90 dias, prorrogável por mais 90 dias (CLT Art. 445 a 451).
                </p>
                <p class="text-sm text-gray-600">
                    <strong>Art. 479:</strong> Se o empregador quebrar o contrato → multa de 50% sobre salários restantes.
                    <br>
                    <strong>Art. 480:</strong> Se o empregado quebrar → desconto máximo de 50% sobre salários restantes.
                </p>
            </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
                <div class="bg-white rounded-lg shadow-md p-6">
                    <h2 class="text-xl font-bold mb-4">Dados de Entrada</h2>
                    
                    <% if (error) { %>
                        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded mb-4">
                            <%= error %>
                        </div>
                    <% } %>

                    <form method="POST" action="/contrato-experiencia/calcular" class="space-y-4">
                        <% if (typeof csrfToken !== 'undefined' && csrfToken) { %>
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <% } %>
                        <div>
                            <label class="block text-sm font-medium mb-2">Data de Início do Contrato *</label>
                            <input type="date" name="dataInicio" required
                                class="w-full px-4 py-2 border rounded-md">
                        </div>

                        <div>
                            <label class="block text-sm font-medium mb-2">Data Prevista de Término *</label>
                            <input type="date" name="dataFimPrevisto" required
                                class="w-full px-4 py-2 border rounded-md">
                            <p class="text-xs text-gray-500 mt-1">Até quando ia o contrato</p>
                        </div>

                        <div>
                            <label class="block text-sm font-medium mb-2">Data Real de Encerramento *</label>
                            <input type="date" name="dataEncerramento" required
                                class="w-full px-4 py-2 border rounded-md">
                            <p class="text-xs text-gray-500 mt-1">Quando o contrato foi quebrado</p>
                        </div>

                        <div>
                            <label class="block text-sm font-medium mb-2">Base de Cálculo (R$) *</label>
                            <input type="number" name="salarioBase" step="0.01" required
                                class="w-full px-4 py-2 border rounded-md">
                            <p class="text-xs text-gray-500 mt-1">Salário mensal do funcionário</p>
                        </div>

                        <div>
                            <label class="block text-sm font-medium mb-2">Quem Quebrou o Contrato *</label>
                            <select name="quemQuebrou" required class="w-full px-4 py-2 border rounded-md">
                                <option value="empregador">Empregador (Art. 479)</option>
                                <option value="empregado">Empregado (Art. 480)</option>
                            </select>
                            <p class="text-xs text-gray-500 mt-1">Quem rompeu o contrato antecipadamente</p>
                        </div>

                        <button type="submit" class="w-full bg-gradient-to-r from-primary-red to-primary-yellow text-white py-3 rounded-md font-semibold">
                            Calcular Multa
                        </button>
                    </form>
                </div>
            </div>

            <div class="lg:col-span-2">
                <% if (resultado) { %>
                    <div class="bg-white rounded-lg shadow-md p-6">
                        <div class="bg-gradient-to-r <%= resultado.quebradoPeloEmpregador ? 'from-red-500 to-red-700' : 'from-yellow-500 to-yellow-700' %> rounded-lg p-6 text-white mb-6">
                            <h3 class="text-2xl font-bold mb-4">
                                <%= resultado.artigoAplicado %> - Multa de Quebra
                            </h3>
                            <p class="text-4xl font-bold">R$ <%= resultado.valorMulta.toFixed(2) %></p>
                            <p class="text-sm opacity-90 mt-2">
                                <%= resultado.quebradoPeloEmpregador ? 'Quebra pelo Empregador' : 'Quebra pelo Empregado' %>
                            </p>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
                            <div class="bg-blue-50 rounded-lg p-4">
                                <p class="text-sm text-gray-600">Dias Restantes</p>
                                <p class="text-xl font-bold"><%= resultado.diasRestantes %> dias</p>
                                <p class="text-xs text-gray-500 mt-1">Período que faltava</p>
                            </div>
                            <div class="bg-green-50 rounded-lg p-4">
                                <p class="text-sm text-gray-600">Valor dos Salários Restantes</p>
                                <p class="text-xl font-bold">R$ <%= resultado.valorSalariosRestantes.toFixed(2) %></p>
                                <p class="text-xs text-gray-500 mt-1">Base de cálculo</p>
                            </div>
                            <div class="bg-purple-50 rounded-lg p-4">
                                <p class="text-sm text-gray-600">Artigo Aplicado</p>
                                <p class="text-xl font-bold"><%= resultado.artigoAplicado %></p>
                                <p class="text-xs text-gray-500 mt-1">CLT</p>
                            </div>
                        </div>

                        <h4 class="text-xl font-bold mb-4">Memória de Cálculo</h4>
                        <div class="space-y-3">
                            <% resultado.memoria.forEach(function(passo) { %>
                                <% var classeDiv = passo.destaque ? 'border-primary-yellow bg-yellow-50' : 'border-gray-300'; %>
                                <div class="bg-gray-50 rounded-lg p-4 border-l-4 <%= classeDiv %>">
                                    <p class="font-semibold"><%= passo.descricao %></p>
                                    <% if (passo.calculo) { %>
                                        <p class="text-sm text-gray-600 mt-1"><%= passo.calculo %></p>
                                    <% } %>
                                    <% if (passo.valor) { %>
                                        <p class="text-primary-red font-bold mt-2"><%= passo.valor %></p>
                                    <% } %>
                                </div>
                            <% }); %>
                        </div>

                        <div class="mt-6 bg-gray-50 rounded-lg p-6 border-t-4 border-primary-red">
                            <h4 class="font-bold mb-2">Base Legal</h4>
                            <p class="font-semibold"><%= resultado.baseLegal.titulo %></p>
                            <p class="text-sm text-gray-700 mb-3">
                                <strong>Artigo:</strong> <%= resultado.baseLegal.artigo %>
                            </p>
                            <% if (resultado.baseLegal.textoCompleto) { %>
                                <div class="mt-4 text-sm text-gray-700 leading-relaxed bg-white rounded p-4 border whitespace-pre-line">
                                    <%= resultado.baseLegal.textoCompleto %>
                                </div>
                            <% } else { %>
                                <p class="text-sm text-gray-600"><%= resultado.baseLegal.descricao %></p>
                            <% } %>
                        </div>
                    </div>
                <% } else { %>
                    <div class="bg-white rounded-lg shadow-md p-12 text-center">
                        <i class="fas fa-calculator text-gray-300 text-6xl mb-4"></i>
                        <p class="text-gray-600">Preencha os dados para calcular a multa</p>
                    </div>
                <% } %>
            </div>
        </div>
    </main>
</body>
</html>
