<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: { red: '#DC2626', yellow: '#FBBF24' }
                    }
                }
            }
        }
    </script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-50">
    <% if (typeof user !== 'undefined' && user) { %>
        <%- include('../partials/navbar') %>
    <% } %>

    <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
        <div class="mb-6">
            <h1 class="text-3xl font-bold text-gray-900 mb-2">
                <i class="fas fa-file-contract text-primary-yellow mr-2"></i>
                Quebra de Contrato de Experiência
            </h1>
            <div class="bg-blue-50 border-l-4 border-blue-400 p-4 rounded">
                <p class="text-gray-700 mb-2">
                    <i class="fas fa-info-circle text-blue-500 mr-2"></i>
                    <strong>O que é Contrato de Experiência?</strong>
                </p>
                <p class="text-sm text-gray-600">
                    Contrato de experiência pode durar até 90 dias, prorrogável por mais 90 dias. 
                    Quebra antecipada gera direito a verbas rescisórias proporcionais conforme CLT Art. 445 a 451.
                </p>
            </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
                <div class="bg-white rounded-lg shadow-md p-6">
                    <h2 class="text-xl font-bold mb-4">Dados de Entrada</h2>
                    
                    <% if (error) { %>
                        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded mb-4">
                            <%= error %>
                        </div>
                    <% } %>

                    <form method="POST" action="/contrato-experiencia/calcular" class="space-y-4">
                        <div>
                            <label class="block text-sm font-medium mb-2">Data de Início do Contrato *</label>
                            <input type="date" name="dataInicio" required
                                class="w-full px-4 py-2 border rounded-md">
                        </div>

                        <div>
                            <label class="block text-sm font-medium mb-2">Data Prevista de Término *</label>
                            <input type="date" name="dataFimPrevisto" required
                                class="w-full px-4 py-2 border rounded-md">
                            <p class="text-xs text-gray-500 mt-1">Até quando ia o contrato</p>
                        </div>

                        <div>
                            <label class="block text-sm font-medium mb-2">Data Real de Encerramento *</label>
                            <input type="date" name="dataEncerramento" required
                                class="w-full px-4 py-2 border rounded-md">
                            <p class="text-xs text-gray-500 mt-1">Quando encerrou</p>
                        </div>

                        <div>
                            <label class="block text-sm font-medium mb-2">Salário Base (R$) *</label>
                            <input type="number" name="salarioBase" step="0.01" required
                                class="w-full px-4 py-2 border rounded-md">
                        </div>

                        <div>
                            <label class="block text-sm font-medium mb-2">Valor das Médias (R$)</label>
                            <input type="number" name="valorMedias" step="0.01" value="0"
                                class="w-full px-4 py-2 border rounded-md">
                            <p class="text-xs text-gray-500 mt-1">Médias de horas extras, comissões, etc.</p>
                        </div>

                        <button type="submit" class="w-full bg-gradient-to-r from-primary-red to-primary-yellow text-white py-3 rounded-md font-semibold">
                            Calcular
                        </button>
                    </form>
                </div>
            </div>

            <div class="lg:col-span-2">
                <% if (resultado) { %>
                    <div class="bg-white rounded-lg shadow-md p-6">
                        <div class="bg-gradient-to-r from-primary-yellow via-primary-red to-primary-yellow rounded-lg p-6 text-white mb-6">
                            <h3 class="text-2xl font-bold mb-4">Valor Total das Verbas Rescisórias</h3>
                            <p class="text-4xl font-bold">R$ <%= resultado.valorTotal.toFixed(2) %></p>
                            <p class="text-sm opacity-90 mt-2">
                                <% if (resultado.quebradoPeloEmpregador) { %>
                                    Quebra pelo Empregador
                                <% } else { %>
                                    Quebra pelo Empregado
                                <% } %>
                            </p>
                        </div>

                        <div class="grid grid-cols-2 md:grid-cols-3 gap-4 mb-6">
                            <div class="bg-blue-50 rounded-lg p-4">
                                <p class="text-sm text-gray-600">Aviso Prévio</p>
                                <p class="text-xl font-bold">R$ <%= resultado.valorAvisoPrevio.toFixed(2) %></p>
                            </div>
                            <div class="bg-green-50 rounded-lg p-4">
                                <p class="text-sm text-gray-600">13º Proporcional</p>
                                <p class="text-xl font-bold">R$ <%= resultado.valor13Proporcional.toFixed(2) %></p>
                            </div>
                            <div class="bg-yellow-50 rounded-lg p-4">
                                <p class="text-sm text-gray-600">Férias Proporcionais</p>
                                <p class="text-xl font-bold">R$ <%= resultado.valorFeriasProporcionais.toFixed(2) %></p>
                            </div>
                            <div class="bg-purple-50 rounded-lg p-4">
                                <p class="text-sm text-gray-600">1/3 Férias</p>
                                <p class="text-xl font-bold">R$ <%= resultado.valorTercoFerias.toFixed(2) %></p>
                            </div>
                            <div class="bg-orange-50 rounded-lg p-4">
                                <p class="text-sm text-gray-600">FGTS</p>
                                <p class="text-xl font-bold">R$ <%= resultado.valorFgts.toFixed(2) %></p>
                            </div>
                            <div class="bg-red-50 rounded-lg p-4">
                                <p class="text-sm text-gray-600">Multa FGTS (40%)</p>
                                <p class="text-xl font-bold">R$ <%= resultado.valorMultaFgts.toFixed(2) %></p>
                            </div>
                        </div>

                        <h4 class="text-xl font-bold mb-4">Memória de Cálculo</h4>
                        <div class="space-y-3">
                            <% resultado.memoria.forEach(function(passo) { %>
                                <% var classeDiv = passo.destaque ? 'border-primary-yellow bg-yellow-50' : 'border-gray-300'; %>
                                <div class="bg-gray-50 rounded-lg p-4 border-l-4 <%= classeDiv %>">
                                    <p class="font-semibold"><%= passo.descricao %></p>
                                    <% if (passo.calculo) { %>
                                        <p class="text-sm text-gray-600 mt-1"><%= passo.calculo %></p>
                                    <% } %>
                                    <% if (passo.valor) { %>
                                        <p class="text-primary-red font-bold mt-2"><%= passo.valor %></p>
                                    <% } %>
                                </div>
                            <% }); %>
                        </div>
                    </div>
                <% } else { %>
                    <div class="bg-white rounded-lg shadow-md p-12 text-center">
                        <i class="fas fa-calculator text-gray-300 text-6xl mb-4"></i>
                        <p class="text-gray-600">Preencha os dados para calcular</p>
                    </div>
                <% } %>
            </div>
        </div>
    </main>
</body>
</html>

