<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: { red: '#DC2626', yellow: '#FBBF24' }
                    },
                    animation: {
                        'float': 'float 6s ease-in-out infinite',
                        'pulse-slow': 'pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite',
                        'gradient': 'gradient 15s ease infinite',
                        'slide-up': 'slideUp 0.5s ease-out',
                        'fade-in': 'fadeIn 0.6s ease-out',
                    },
                    keyframes: {
                        float: {
                            '0%, 100%': { transform: 'translateY(0px)' },
                            '50%': { transform: 'translateY(-20px)' }
                        },
                        gradient: {
                            '0%, 100%': { backgroundPosition: '0% 50%' },
                            '50%': { backgroundPosition: '100% 50%' }
                        },
                        slideUp: {
                            '0%': { transform: 'translateY(30px)', opacity: '0' },
                            '100%': { transform: 'translateY(0)', opacity: '1' }
                        },
                        fadeIn: {
                            '0%': { opacity: '0' },
                            '100%': { opacity: '1' }
                        }
                    }
                }
            }
        }
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/loading.css">
    <!-- Favicon com as letras "DP" nas cores do sistema (Vermelho #DC2626 e Amarelo #FBBF24) -->
    <link rel="icon" type="image/svg+xml"
        href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 200 200'%3E%3Cdefs%3E%3ClinearGradient id='grad' x1='0%25' y1='0%25' x2='100%25' y2='100%25'%3E%3Cstop offset='0%25' style='stop-color:%23DC2626;stop-opacity:1' /%3E%3Cstop offset='100%25' style='stop-color:%23FBBF24;stop-opacity:1' /%3E%3C/linearGradient%3E%3C/defs%3E%3Crect width='200' height='200' rx='20' fill='url(%23grad)'/%3E%3Ctext x='100' y='140' font-family='Arial, sans-serif' font-size='120' font-weight='bold' text-anchor='middle' fill='white'%3EDP%3C/text%3E%3C/svg%3E" />
    <link rel="apple-touch-icon"
        href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 200 200'%3E%3Cdefs%3E%3ClinearGradient id='grad' x1='0%25' y1='0%25' x2='100%25' y2='100%25'%3E%3Cstop offset='0%25' style='stop-color:%23DC2626;stop-opacity:1' /%3E%3Cstop offset='100%25' style='stop-color:%23FBBF24;stop-opacity:1' /%3E%3C/linearGradient%3E%3C/defs%3E%3Crect width='200' height='200' rx='20' fill='url(%23grad)'/%3E%3Ctext x='100' y='140' font-family='Arial, sans-serif' font-size='120' font-weight='bold' text-anchor='middle' fill='white'%3EDP%3C/text%3E%3C/svg%3E" />
    <style>
        * {
            font-family: 'Inter', sans-serif;
        }

        .glass {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .glass-dark {
            background: rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
        }

        .gradient-text {
            background: linear-gradient(135deg, #DC2626 0%, #FBBF24 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .gradient-bg {
            background: linear-gradient(135deg, #DC2626 0%, #FBBF24 50%, #DC2626 100%);
            background-size: 200% 200%;
            animation: gradient 15s ease infinite;
        }

        .pattern-dots {
            background-image: radial-gradient(circle, rgba(220, 38, 38, 0.1) 1px, transparent 1px);
            background-size: 20px 20px;
        }

        .pattern-grid {
            background-image:
                linear-gradient(rgba(220, 38, 38, 0.05) 1px, transparent 1px),
                linear-gradient(90deg, rgba(220, 38, 38, 0.05) 1px, transparent 1px);
            background-size: 30px 30px;
        }

        .icon-glow {
            filter: drop-shadow(0 0 10px rgba(220, 38, 38, 0.5));
        }

        .icon-glow-yellow {
            filter: drop-shadow(0 0 10px rgba(251, 191, 36, 0.5));
        }

        .input-glow:focus {
            box-shadow: 0 0 0 3px rgba(220, 38, 38, 0.1), 0 0 20px rgba(220, 38, 38, 0.2);
        }

        .card-hover {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .card-hover:hover {
            transform: translateY(-4px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }
    </style>
</head>

<body class="bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 min-h-screen">
    <%- include('../partials/navbar') %>

        <!-- Loading Overlay -->
        <div id="loadingOverlay" class="loading-overlay">
            <div class="spinner"></div>
        </div>

        <main class="max-w-5xl mx-auto py-8 sm:px-6 lg:px-8 relative">
            <!-- Background Elements -->
            <div class="absolute top-0 right-0 w-96 h-96 bg-primary-red/10 rounded-full blur-3xl"></div>
            <div class="absolute bottom-0 left-0 w-96 h-96 bg-primary-yellow/10 rounded-full blur-3xl"></div>

            <div class="relative z-10">
                <div class="mb-8 animate-fade-in">
                    <div class="flex items-center space-x-4 mb-4">
                        <div class="relative">
                            <div
                                class="absolute inset-0 bg-gradient-to-r from-primary-red to-primary-yellow rounded-2xl blur-xl opacity-50">
                            </div>
                            <div class="relative bg-gradient-to-r from-primary-red to-primary-yellow p-3 rounded-2xl">
                                <i class="fas fa-user-circle text-white text-3xl icon-glow"></i>
                            </div>
                        </div>
                        <div>
                            <h1 class="text-4xl font-black text-white">
                                Meu <span class="gradient-text">Perfil</span>
                            </h1>
                            <p class="mt-1 text-white/70 text-sm font-medium">Gerencie suas informações pessoais</p>
                        </div>
                    </div>
                </div>

                <% if (error) { %>
                    <div
                        class="glass-dark border-l-4 border-red-500 text-red-200 px-4 py-3 rounded-lg mb-6 flex items-start animate-slide-up">
                        <i class="fas fa-exclamation-circle mr-2 mt-0.5 flex-shrink-0"></i>
                        <div>
                            <strong class="block">Erro:</strong>
                            <span class="text-sm">
                                <%= error %>
                            </span>
                        </div>
                    </div>
                    <% } %>

                        <% if (success) { %>
                            <div
                                class="glass-dark border-l-4 border-green-500 text-green-200 px-4 py-3 rounded-lg mb-6 flex items-start animate-slide-up">
                                <i class="fas fa-check-circle mr-2 mt-0.5 flex-shrink-0"></i>
                                <div>
                                    <strong class="block">Sucesso:</strong>
                                    <span class="text-sm">
                                        <%= success %>
                                    </span>
                                </div>
                            </div>
                            <% } %>

                                <!-- Dados Básicos -->
                                <div
                                    class="glass rounded-2xl shadow-2xl p-6 sm:p-8 mb-6 border border-white/20 backdrop-blur-xl card-hover animate-slide-up">
                                    <div class="flex items-center mb-6">
                                        <div class="relative mr-3">
                                            <div
                                                class="absolute inset-0 bg-gradient-to-r from-primary-red to-primary-yellow rounded-xl blur opacity-50">
                                            </div>
                                            <div
                                                class="relative bg-gradient-to-r from-primary-red to-primary-yellow p-2 rounded-xl">
                                                <i class="fas fa-user text-white text-xl icon-glow"></i>
                                            </div>
                                        </div>
                                        <h2 class="text-2xl font-black text-white">
                                            Dados <span class="gradient-text">Básicos</span>
                                        </h2>
                                    </div>
                                    <form action="/perfil/update-basic" method="POST"
                                        onsubmit="showLoading('Salvando dados básicos...')">
                                        <% if (typeof csrfToken !=='undefined' && csrfToken) { %>
                                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                            <% } %>
                                                <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
                                                    <div>
                                                        <label class="block text-sm font-semibold text-white/90 mb-2">
                                                            <i class="fas fa-user text-primary-yellow mr-2"></i>Nome
                                                        </label>
                                                        <input type="text" name="nome" value="<%= user.nome %>" required
                                                            class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-primary-red focus:border-primary-red transition-all input-glow backdrop-blur-sm">
                                                    </div>
                                                    <div>
                                                        <label class="block text-sm font-semibold text-white/90 mb-2">
                                                            <i
                                                                class="fas fa-envelope text-primary-yellow mr-2"></i>Email
                                                        </label>
                                                        <input type="email" name="email" value="<%= user.email %>"
                                                            required
                                                            class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-primary-red focus:border-primary-red transition-all input-glow backdrop-blur-sm">
                                                    </div>
                                                </div>
                                                <div class="mt-6">
                                                    <button type="submit"
                                                        class="group relative bg-gradient-to-r from-primary-red to-primary-yellow text-white px-8 py-3 rounded-xl font-bold hover:shadow-2xl hover:shadow-primary-red/50 transition-all transform hover:scale-105 overflow-hidden">
                                                        <span class="relative z-10 flex items-center">
                                                            <i class="fas fa-save mr-2"></i>Salvar Dados Básicos
                                                            <i
                                                                class="fas fa-arrow-right ml-2 group-hover:translate-x-1 transition-transform"></i>
                                                        </span>
                                                        <div
                                                            class="absolute inset-0 bg-gradient-to-r from-primary-yellow to-primary-red opacity-0 group-hover:opacity-100 transition-opacity">
                                                        </div>
                                                    </button>
                                                </div>
                                    </form>
                                </div>

                                <!-- Dados Adicionais -->
                                <div class="glass rounded-2xl shadow-2xl p-6 sm:p-8 mb-6 border border-white/20 backdrop-blur-xl card-hover animate-slide-up"
                                    style="animation-delay: 0.1s;">
                                    <div class="flex items-center mb-6">
                                        <div class="relative mr-3">
                                            <div
                                                class="absolute inset-0 bg-gradient-to-r from-primary-yellow to-primary-red rounded-xl blur opacity-50">
                                            </div>
                                            <div
                                                class="relative bg-gradient-to-r from-primary-yellow to-primary-red p-2 rounded-xl">
                                                <i class="fas fa-address-card text-white text-xl icon-glow-yellow"></i>
                                            </div>
                                        </div>
                                        <h2 class="text-2xl font-black text-white">
                                            Informações <span class="gradient-text">Adicionais</span>
                                        </h2>
                                    </div>
                                    <p
                                        class="text-sm text-white/70 mb-6 bg-white/5 p-3 rounded-lg border border-white/10">
                                        <i class="fas fa-info-circle text-primary-yellow mr-2"></i>
                                        Essas informações são privadas e podem ser úteis para contato
                                        comercial/marketing.
                                    </p>
                                    <form action="/perfil/update-profile" method="POST"
                                        onsubmit="showLoading('Salvando informações adicionais...')">
                                        <% if (typeof csrfToken !=='undefined' && csrfToken) { %>
                                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                            <% } %>
                                                <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
                                                    <div>
                                                        <label class="block text-sm font-semibold text-white/90 mb-2">
                                                            <i
                                                                class="fas fa-phone text-primary-yellow mr-2"></i>Telefone
                                                        </label>
                                                        <input type="tel" name="telefone"
                                                            value="<%= user.telefone || '' %>"
                                                            placeholder="(00) 0000-0000"
                                                            class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-primary-red focus:border-primary-red transition-all input-glow backdrop-blur-sm">
                                                    </div>
                                                    <div>
                                                        <label class="block text-sm font-semibold text-white/90 mb-2">
                                                            <i
                                                                class="fab fa-whatsapp text-primary-yellow mr-2"></i>WhatsApp
                                                        </label>
                                                        <input type="tel" name="whatsapp"
                                                            value="<%= user.whatsapp || '' %>"
                                                            placeholder="(00) 00000-0000"
                                                            class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-primary-red focus:border-primary-red transition-all input-glow backdrop-blur-sm">
                                                    </div>
                                                    <div>
                                                        <label class="block text-sm font-semibold text-white/90 mb-2">
                                                            <i
                                                                class="fas fa-building text-primary-yellow mr-2"></i>Empresa
                                                        </label>
                                                        <input type="text" name="empresa"
                                                            value="<%= user.empresa || '' %>"
                                                            placeholder="Nome da empresa"
                                                            class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-primary-red focus:border-primary-red transition-all input-glow backdrop-blur-sm">
                                                    </div>
                                                    <div>
                                                        <label class="block text-sm font-semibold text-white/90 mb-2">
                                                            <i
                                                                class="fas fa-briefcase text-primary-yellow mr-2"></i>Cargo
                                                        </label>
                                                        <input type="text" name="cargo" value="<%= user.cargo || '' %>"
                                                            placeholder="Seu cargo/função"
                                                            class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-primary-red focus:border-primary-red transition-all input-glow backdrop-blur-sm">
                                                    </div>
                                                    <div class="md:col-span-2">
                                                        <label class="block text-sm font-semibold text-white/90 mb-2">
                                                            <i
                                                                class="fab fa-instagram text-primary-yellow mr-2"></i>Instagram
                                                        </label>
                                                        <input type="text" name="instagram"
                                                            value="<%= user.instagram || '' %>"
                                                            placeholder="@seuinstagram"
                                                            class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-primary-red focus:border-primary-red transition-all input-glow backdrop-blur-sm">
                                                    </div>
                                                    <div class="md:col-span-2">
                                                        <label class="block text-sm font-semibold text-white/90 mb-2">
                                                            <i
                                                                class="fas fa-sticky-note text-primary-yellow mr-2"></i>Observações
                                                        </label>
                                                        <textarea name="observacoes" rows="4"
                                                            placeholder="Observações adicionais..."
                                                            class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-primary-red focus:border-primary-red transition-all input-glow backdrop-blur-sm resize-none"><%= user.observacoes || '' %></textarea>
                                                    </div>
                                                </div>
                                                <div class="mt-6">
                                                    <button type="submit"
                                                        class="group relative bg-gradient-to-r from-primary-yellow to-primary-red text-white px-8 py-3 rounded-xl font-bold hover:shadow-2xl hover:shadow-primary-yellow/50 transition-all transform hover:scale-105 overflow-hidden">
                                                        <span class="relative z-10 flex items-center">
                                                            <i class="fas fa-save mr-2"></i>Salvar Informações
                                                            Adicionais
                                                            <i
                                                                class="fas fa-arrow-right ml-2 group-hover:translate-x-1 transition-transform"></i>
                                                        </span>
                                                        <div
                                                            class="absolute inset-0 bg-gradient-to-r from-primary-red to-primary-yellow opacity-0 group-hover:opacity-100 transition-opacity">
                                                        </div>
                                                    </button>
                                                </div>
                                    </form>
                                </div>

                                <!-- Alterar Senha -->
                                <div class="glass rounded-2xl shadow-2xl p-6 sm:p-8 mb-6 border border-white/20 backdrop-blur-xl card-hover animate-slide-up"
                                    style="animation-delay: 0.2s;">
                                    <div class="flex items-center mb-6">
                                        <div class="relative mr-3">
                                            <div
                                                class="absolute inset-0 bg-gradient-to-r from-primary-red to-primary-yellow rounded-xl blur opacity-50">
                                            </div>
                                            <div
                                                class="relative bg-gradient-to-r from-primary-red to-primary-yellow p-2 rounded-xl">
                                                <i class="fas fa-lock text-white text-xl icon-glow"></i>
                                            </div>
                                        </div>
                                        <h2 class="text-2xl font-black text-white">
                                            Alterar <span class="gradient-text">Senha</span>
                                        </h2>
                                    </div>
                                    <form action="/perfil/update-password" method="POST"
                                        onsubmit="showLoading('Alterando senha...')">
                                        <% if (typeof csrfToken !=='undefined' && csrfToken) { %>
                                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                            <% } %>
                                                <div class="grid grid-cols-1 md:grid-cols-3 gap-5">
                                                    <div>
                                                        <label class="block text-sm font-semibold text-white/90 mb-2">
                                                            <i class="fas fa-key text-primary-yellow mr-2"></i>Senha
                                                            Atual
                                                        </label>
                                                        <input type="password" name="senhaAtual" required
                                                            class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-primary-red focus:border-primary-red transition-all input-glow backdrop-blur-sm"
                                                            placeholder="Digite sua senha atual">
                                                    </div>
                                                    <div>
                                                        <label class="block text-sm font-semibold text-white/90 mb-2">
                                                            <i class="fas fa-lock text-primary-yellow mr-2"></i>Nova
                                                            Senha
                                                        </label>
                                                        <input type="password" name="novaSenha" required minlength="6"
                                                            class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-primary-red focus:border-primary-red transition-all input-glow backdrop-blur-sm"
                                                            placeholder="Mínimo 6 caracteres">
                                                    </div>
                                                    <div>
                                                        <label class="block text-sm font-semibold text-white/90 mb-2">
                                                            <i
                                                                class="fas fa-lock text-primary-yellow mr-2"></i>Confirmar
                                                            Nova Senha
                                                        </label>
                                                        <input type="password" name="confirmarNovaSenha" required
                                                            minlength="6"
                                                            class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-primary-red focus:border-primary-red transition-all input-glow backdrop-blur-sm"
                                                            placeholder="Confirme a nova senha">
                                                    </div>
                                                </div>
                                                <div class="mt-6">
                                                    <button type="submit"
                                                        class="group relative bg-gradient-to-r from-gray-700 to-gray-800 text-white px-8 py-3 rounded-xl font-bold hover:shadow-2xl hover:shadow-gray-700/50 transition-all transform hover:scale-105 overflow-hidden">
                                                        <span class="relative z-10 flex items-center">
                                                            <i class="fas fa-key mr-2"></i>Alterar Senha
                                                            <i
                                                                class="fas fa-arrow-right ml-2 group-hover:translate-x-1 transition-transform"></i>
                                                        </span>
                                                        <div
                                                            class="absolute inset-0 bg-gradient-to-r from-gray-800 to-gray-700 opacity-0 group-hover:opacity-100 transition-opacity">
                                                        </div>
                                                    </button>
                                                </div>
                                    </form>
                                </div>
            </div>
        </main>

        <script src="/js/loading.js"></script>
</body>

</html>