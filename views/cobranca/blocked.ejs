<%- include('../layout', { title: title || 'Acesso Bloqueado' }) %>

<div class="container mx-auto px-4 py-8">
  <div class="max-w-2xl mx-auto">
    <div class="bg-red-50 border-l-4 border-red-500 p-6 rounded-lg shadow-lg">
      <div class="flex items-center mb-4">
        <svg class="w-12 h-12 text-red-500 mr-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
        </svg>
        <h1 class="text-3xl font-bold text-red-700">Acesso Bloqueado</h1>
      </div>

      <div class="bg-white p-6 rounded-lg mb-6">
        <p class="text-gray-700 mb-4">
          Olá, <strong><%= user.nome %></strong>!
        </p>
        <p class="text-gray-700 mb-4">
          Seu acesso foi bloqueado devido ao atraso no pagamento da mensalidade.
        </p>

        <% if (cobranca) { %>
        <div class="bg-gray-50 p-4 rounded-lg mb-4">
          <h3 class="font-semibold text-gray-800 mb-2">Detalhes da Cobrança:</h3>
          <p class="text-gray-700"><strong>Valor:</strong> R$ <%= parseFloat(cobranca.valor).toFixed(2) %></p>
          <p class="text-gray-700"><strong>Vencimento:</strong> <%= new Date(cobranca.data_vencimento).toLocaleDateString('pt-BR') %></p>
          <p class="text-gray-700"><strong>Status:</strong> 
            <span class="px-2 py-1 bg-red-100 text-red-800 rounded">
              <%= cobranca.status === 'vencida' ? 'Vencida' : 'Pendente' %>
            </span>
          </p>
        </div>

        <% if (cobranca.link_pagamento) { %>
        <div class="text-center">
          <a href="<%= cobranca.link_pagamento %>" 
             target="_blank"
             class="inline-block bg-red-600 hover:bg-red-700 text-white font-bold py-3 px-6 rounded-lg transition duration-200">
            Pagar e Reativar Acesso
          </a>
        </div>
        <% } %>
        <% } else { %>
        <p class="text-gray-600 mb-4">
          Entre em contato com o suporte para regularizar sua situação.
        </p>
        <% } %>
      </div>

      <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4 rounded">
        <p class="text-yellow-800 text-sm">
          <strong>⚠️ Importante:</strong> Após a confirmação do pagamento, seu acesso será liberado automaticamente.
        </p>
      </div>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>

