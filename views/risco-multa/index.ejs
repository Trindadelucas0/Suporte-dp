<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: { red: '#DC2626', yellow: '#FBBF24' }
                    }
                }
            }
        }
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bungee+Inline&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .bungee-inline-regular {
            font-family: "Bungee Inline", sans-serif;
            font-weight: 400;
            font-style: normal;
        }
    </style>
    <!-- Favicon com as letras "DP" nas cores do sistema (Vermelho #DC2626 e Amarelo #FBBF24) -->
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 200 200'%3E%3Cdefs%3E%3ClinearGradient id='grad' x1='0%25' y1='0%25' x2='100%25' y2='100%25'%3E%3Cstop offset='0%25' style='stop-color:%23DC2626;stop-opacity:1' /%3E%3Cstop offset='100%25' style='stop-color:%23FBBF24;stop-opacity:1' /%3E%3C/linearGradient%3E%3C/defs%3E%3Crect width='200' height='200' rx='20' fill='url(%23grad)'/%3E%3Ctext x='100' y='140' font-family='Arial, sans-serif' font-size='120' font-weight='bold' text-anchor='middle' fill='white'%3EDP%3C/text%3E%3C/svg%3E" />
    <link rel="apple-touch-icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 200 200'%3E%3Cdefs%3E%3ClinearGradient id='grad' x1='0%25' y1='0%25' x2='100%25' y2='100%25'%3E%3Cstop offset='0%25' style='stop-color:%23DC2626;stop-opacity:1' /%3E%3Cstop offset='100%25' style='stop-color:%23FBBF24;stop-opacity:1' /%3E%3C/linearGradient%3E%3C/defs%3E%3Crect width='200' height='200' rx='20' fill='url(%23grad)'/%3E%3Ctext x='100' y='140' font-family='Arial, sans-serif' font-size='120' font-weight='bold' text-anchor='middle' fill='white'%3EDP%3C/text%3E%3C/svg%3E" />
</head>
<body class="bg-gray-50">
    <% if (typeof user !== 'undefined' && user) { %>
        <%- include('../partials/navbar') %>
    <% } %>

    <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
        <div class="mb-6">
            <h1 class="text-3xl font-bold text-gray-900 mb-2">
                <i class="fas fa-exclamation-triangle text-primary-yellow mr-2"></i>
                Período de Risco Multa (Data Base)
            </h1>
            <div class="bg-blue-50 border-l-4 border-blue-400 p-4 rounded">
                <p class="text-gray-700 mb-2">
                    <i class="fas fa-info-circle text-blue-500 mr-2"></i>
                    <strong>O que é o Período de Risco?</strong>
                </p>
                <p class="text-sm text-gray-600">
                    O período de risco é de <strong>exatamente 30 dias que antecedem a data base</strong> do contrato. 
                    Se a dispensa SEM JUSTA CAUSA ocorrer dentro deste período, o empregado terá direito à indenização adicional equivalente a um salário mensal (valor integral) - Lei nº 7.238/84 – Art 9º.
                </p>
            </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
            <!-- Formulário -->
            <div class="lg:col-span-1">
                <div class="bg-white rounded-lg shadow-md p-6">
                    <h2 class="text-xl font-bold mb-4">
                        <i class="fas fa-edit text-primary-red mr-2"></i>
                        Dados de Entrada
                    </h2>
                    
                    <% if (error) { %>
                        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded mb-4">
                            <i class="fas fa-exclamation-circle mr-2"></i>
                            <%= error %>
                        </div>
                    <% } %>

                    <form method="POST" action="/risco-multa/calcular" class="space-y-4">
                        <% if (typeof csrfToken !== 'undefined' && csrfToken) { %>
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <% } %>

                        <!-- Dados do Sindicato -->
                        <div>
                            <h3 class="text-sm font-semibold text-gray-700 mb-3 uppercase">Dados do Sindicato</h3>
                            
                            <div class="mb-4">
                                <label class="block text-sm font-medium mb-2">Nome do Sindicato *</label>
                                <input type="text" name="nomeSindicato" required
                                    placeholder="Ex: Sindicato dos Trabalhadores"
                                    class="w-full px-4 py-2 border rounded-md focus:ring-2 focus:ring-primary-red focus:border-primary-red">
                            </div>

                            <div class="mb-4">
                                <label class="block text-sm font-medium mb-2">CNPJ do Sindicato *</label>
                                <input type="text" name="cnpjSindicato" required
                                    placeholder="00.000.000/0000-00"
                                    maxlength="18"
                                    class="w-full px-4 py-2 border rounded-md focus:ring-2 focus:ring-primary-red focus:border-primary-red">
                                <p class="text-xs text-gray-500 mt-1">Formato: XX.XXX.XXX/XXXX-XX</p>
                            </div>
                        </div>

                        <!-- Dados do Contrato -->
                        <div class="border-t pt-4">
                            <h3 class="text-sm font-semibold text-gray-700 mb-3 uppercase">Dados do Contrato</h3>
                            
                            <div class="mb-4">
                                <label class="block text-sm font-medium mb-2">Data Base *</label>
                                <input type="date" name="dataBase" required
                                    class="w-full px-4 py-2 border rounded-md focus:ring-2 focus:ring-primary-red focus:border-primary-red">
                                <p class="text-xs text-gray-500 mt-1">Data base do contrato</p>
                            </div>
                        </div>

                        <button type="submit" class="w-full bg-gradient-to-r from-primary-red to-primary-yellow text-white py-3 rounded-md font-semibold hover:opacity-90 transition transform hover:scale-105">
                            <i class="fas fa-calculator mr-2"></i>
                            Calcular Período de Risco
                        </button>
                    </form>
                </div>
            </div>

            <!-- Resultado -->
            <div class="lg:col-span-2">
                <% if (resultado) { %>
                    <div class="bg-white rounded-lg shadow-md p-6">
                        <!-- Header do Resultado -->
                        <div class="bg-gradient-to-r from-primary-red to-primary-yellow rounded-lg p-6 text-white mb-6">
                            <h3 class="text-2xl font-bold mb-4">
                                <i class="fas fa-calendar-alt mr-2"></i>
                                Período de Risco Calculado
                            </h3>
                            <p class="text-lg font-semibold">
                                Período em que pode ocorrer a multa da data base
                            </p>
                        </div>

                        <!-- Informações do Sindicato -->
                        <div class="bg-gray-50 rounded-lg p-4 mb-6">
                            <h4 class="font-bold text-gray-900 mb-3">
                                <i class="fas fa-building text-primary-red mr-2"></i>
                                Dados do Sindicato
                            </h4>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                <div>
                                    <p class="text-sm text-gray-600">Nome</p>
                                    <p class="font-semibold text-gray-900"><%= resultado.nomeSindicato %></p>
                                </div>
                                <div>
                                    <p class="text-sm text-gray-600">CNPJ</p>
                                    <p class="font-semibold text-gray-900"><%= resultado.cnpjSindicato %></p>
                                </div>
                            </div>
                        </div>

                        <!-- Período de Risco -->
                        <div class="bg-red-50 border-l-4 border-primary-red rounded-lg p-6 mb-6">
                            <h4 class="font-bold text-red-900 mb-4 text-lg">
                                <i class="fas fa-exclamation-triangle mr-2"></i>
                                Período de Risco (30 dias que antecedem a data base)
                            </h4>
                            <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
                                <div class="bg-white rounded-lg p-4">
                                    <p class="text-sm text-gray-600 mb-1">Data Base</p>
                                    <p class="text-xl font-bold text-gray-900">
                                        <%= new Date(resultado.dataBase + 'T00:00:00').toLocaleDateString('pt-BR') %>
                                    </p>
                                </div>
                                <div class="bg-white rounded-lg p-4">
                                    <p class="text-sm text-gray-600 mb-1">Início do Risco</p>
                                    <p class="text-xl font-bold text-primary-red">
                                        <%= new Date(resultado.dataInicioRisco + 'T00:00:00').toLocaleDateString('pt-BR') %>
                                    </p>
                                </div>
                                <div class="bg-white rounded-lg p-4">
                                    <p class="text-sm text-gray-600 mb-1">Fim do Risco</p>
                                    <p class="text-xl font-bold text-primary-red">
                                        <%= new Date(resultado.dataFimRisco + 'T00:00:00').toLocaleDateString('pt-BR') %>
                                    </p>
                                </div>
                            </div>
                            <div class="bg-yellow-100 border border-yellow-300 rounded-lg p-4 mb-4">
                                <p class="text-sm font-semibold text-yellow-900 mb-2">
                                    <i class="fas fa-exclamation-triangle mr-2"></i>
                                    Atenção:
                                </p>
                                <p class="text-yellow-800 mb-2">
                                    Se a <strong>dispensa SEM JUSTA CAUSA</strong> ocorrer entre 
                                    <strong><%= new Date(resultado.dataInicioRisco + 'T00:00:00').toLocaleDateString('pt-BR') %></strong> 
                                    e 
                                    <strong><%= new Date(resultado.dataFimRisco + 'T00:00:00').toLocaleDateString('pt-BR') %></strong>, 
                                    o empregado terá direito à <strong>indenização adicional equivalente a um salário mensal (valor integral)</strong>.
                                </p>
                                <p class="text-xs text-yellow-700 italic">
                                    <strong>Base Legal:</strong> Lei nº 7.238/84 – Art 9º
                                </p>
                            </div>
                            
                            <!-- Observações sobre Aviso Prévio -->
                            <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                                <p class="text-sm font-semibold text-blue-900 mb-2">
                                    <i class="fas fa-info-circle mr-2"></i>
                                    Observação Importante:
                                </p>
                                <div class="text-xs text-blue-800">
                                    <p>
                                        <strong>TST – Súmula 182:</strong> O tempo do aviso prévio, mesmo indenizado, conta-se para efeito da indenização adicional prevista no art. 9º da Lei nº 6.708, de 30.10.1979. (mantida - Res. 121/2003, DJ 19, 20 e 21.11.2003)
                                    </p>
                                </div>
                            </div>
                        </div>

                        <!-- Data de Segurança -->
                        <div class="bg-green-50 border-l-4 border-green-500 rounded-lg p-4 mb-6">
                            <p class="font-bold text-green-900 mb-2">
                                <i class="fas fa-check-circle mr-2"></i>
                                Data de Segurança
                            </p>
                            <p class="text-green-800">
                                A partir de <strong><%= new Date(resultado.dataBase + 'T00:00:00').toLocaleDateString('pt-BR') %></strong> (data base), 
                                não há mais risco de multa por período de risco.
                            </p>
                        </div>

                        <!-- Tabela de Consultas Realizadas -->
                        <% if (typeof consultas !== 'undefined' && consultas && consultas.length > 0) { %>
                            <div class="bg-white rounded-lg shadow-md p-6 mb-6 border border-gray-200">
                                <h4 class="text-xl font-bold mb-4">
                                    <i class="fas fa-table text-primary-red mr-2"></i>
                                    Consultas Realizadas
                                </h4>
                                <div class="overflow-x-auto">
                                    <table class="min-w-full divide-y divide-gray-200">
                                        <thead class="bg-gray-50">
                                            <tr>
                                                <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Sindicato</th>
                                                <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">CNPJ</th>
                                                <th class="px-4 py-3 text-left text-xs font-medium text-blue-600 uppercase tracking-wider">Data Base</th>
                                                <th class="px-4 py-3 text-left text-xs font-medium text-red-600 uppercase tracking-wider">Período de Risco</th>
                                                <th class="px-4 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">Ações</th>
                                            </tr>
                                        </thead>
                                        <tbody class="bg-white divide-y divide-gray-200">
                                            <% consultas.forEach(consulta => { %>
                                                <tr class="hover:bg-gray-50 transition">
                                                    <td class="px-4 py-3 whitespace-nowrap text-sm font-medium text-gray-900">
                                                        <%= consulta.nome_sindicato || '-' %>
                                                    </td>
                                                    <td class="px-4 py-3 whitespace-nowrap text-sm text-gray-600">
                                                        <%= consulta.cnpj_sindicato || '-' %>
                                                    </td>
                                                    <td class="px-4 py-3 whitespace-nowrap text-sm font-bold text-blue-600">
                                                        <% 
                                                            // Função helper para formatar data
                                                            function formatarData(data) {
                                                                if (!data) return '-';
                                                                try {
                                                                    // Se já é string no formato YYYY-MM-DD
                                                                    const dataStr = String(data);
                                                                    if (dataStr.match(/^\d{4}-\d{2}-\d{2}/)) {
                                                                        const partes = dataStr.split('T')[0].split('-');
                                                                        return partes[2] + '/' + partes[1] + '/' + partes[0];
                                                                    }
                                                                    // Tenta como Date
                                                                    const dataObj = new Date(dataStr);
                                                                    if (!isNaN(dataObj.getTime())) {
                                                                        return dataObj.toLocaleDateString('pt-BR');
                                                                    }
                                                                    return dataStr.substring(0, 10);
                                                                } catch(e) {
                                                                    return String(data).substring(0, 10);
                                                                }
                                                            }
                                                        %>
                                                        <%= formatarData(consulta.data_base) %>
                                                    </td>
                                                    <td class="px-4 py-3 whitespace-nowrap text-sm font-bold text-red-600">
                                                        <% 
                                                            let periodoTexto = '-';
                                                            if (consulta.data_inicio_risco && consulta.data_fim_risco) {
                                                                const inicioFormatado = formatarData(consulta.data_inicio_risco);
                                                                const fimFormatado = formatarData(consulta.data_fim_risco);
                                                                if (inicioFormatado !== '-' && fimFormatado !== '-') {
                                                                    periodoTexto = inicioFormatado + ' até ' + fimFormatado;
                                                                }
                                                            }
                                                        %>
                                                        <%= periodoTexto %>
                                                    </td>
                                                    <td class="px-4 py-3 whitespace-nowrap text-center">
                                                        <button 
                                                            type="button"
                                                            class="deletar-consulta-btn bg-red-500 hover:bg-red-600 text-white px-3 py-1 rounded text-sm transition"
                                                            data-id="<%= consulta.id %>"
                                                            title="Excluir consulta">
                                                            <i class="fas fa-trash"></i>
                                                        </button>
                                                    </td>
                                                </tr>
                                            <% }); %>
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        <% } %>
                    </div>
                <% } else { %>
                    <div class="bg-white rounded-lg shadow-md p-12 text-center mb-6">
                        <i class="fas fa-calculator text-gray-300 text-6xl mb-4"></i>
                        <p class="text-gray-600 text-lg">Preencha os dados para calcular o período de risco</p>
                    </div>

                    <!-- Tabela de Consultas Realizadas (sem resultado) -->
                    <% if (typeof consultas !== 'undefined' && consultas && consultas.length > 0) { %>
                        <div class="bg-white rounded-lg shadow-md p-6 border border-gray-200">
                            <h4 class="text-xl font-bold mb-4">
                                <i class="fas fa-table text-primary-red mr-2"></i>
                                Consultas Realizadas
                            </h4>
                            <div class="overflow-x-auto">
                                <table class="min-w-full divide-y divide-gray-200">
                                    <thead class="bg-gray-50">
                                        <tr>
                                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Sindicato</th>
                                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">CNPJ</th>
                                            <th class="px-4 py-3 text-left text-xs font-medium text-blue-600 uppercase tracking-wider">Data Base</th>
                                            <th class="px-4 py-3 text-left text-xs font-medium text-red-600 uppercase tracking-wider">Período de Risco</th>
                                            <th class="px-4 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">Ações</th>
                                        </tr>
                                    </thead>
                                    <tbody class="bg-white divide-y divide-gray-200">
                                        <% 
                                            // Função helper para formatar data (definida uma vez)
                                            function formatarData(data) {
                                                if (!data) return '-';
                                                try {
                                                    const dataStr = String(data);
                                                    if (dataStr.match(/^\d{4}-\d{2}-\d{2}/)) {
                                                        const partes = dataStr.split('T')[0].split('-');
                                                        return partes[2] + '/' + partes[1] + '/' + partes[0];
                                                    }
                                                    const dataObj = new Date(dataStr);
                                                    if (!isNaN(dataObj.getTime())) {
                                                        return dataObj.toLocaleDateString('pt-BR');
                                                    }
                                                    return dataStr.substring(0, 10);
                                                } catch(e) {
                                                    return String(data).substring(0, 10);
                                                }
                                            }
                                        %>
                                        <% consultas.forEach(consulta => { %>
                                            <tr class="hover:bg-gray-50 transition">
                                                <td class="px-4 py-3 whitespace-nowrap text-sm font-medium text-gray-900">
                                                    <%= consulta.nome_sindicato || '-' %>
                                                </td>
                                                <td class="px-4 py-3 whitespace-nowrap text-sm text-gray-600">
                                                    <%= consulta.cnpj_sindicato || '-' %>
                                                </td>
                                                <td class="px-4 py-3 whitespace-nowrap text-sm font-bold text-blue-600">
                                                    <%= formatarData(consulta.data_base) %>
                                                </td>
                                                <td class="px-4 py-3 whitespace-nowrap text-sm font-bold text-red-600">
                                                    <% 
                                                        let periodoTexto2 = '-';
                                                        if (consulta.data_inicio_risco && consulta.data_fim_risco) {
                                                            const inicioFormatado = formatarData(consulta.data_inicio_risco);
                                                            const fimFormatado = formatarData(consulta.data_fim_risco);
                                                            if (inicioFormatado !== '-' && fimFormatado !== '-') {
                                                                periodoTexto2 = inicioFormatado + ' até ' + fimFormatado;
                                                            }
                                                        }
                                                    %>
                                                    <%= periodoTexto2 %>
                                                </td>
                                                <td class="px-4 py-3 whitespace-nowrap text-center">
                                                    <button 
                                                        type="button"
                                                        class="deletar-consulta-btn bg-red-500 hover:bg-red-600 text-white px-3 py-1 rounded text-sm transition"
                                                        data-id="<%= consulta.id %>"
                                                        title="Excluir consulta">
                                                        <i class="fas fa-trash"></i>
                                                    </button>
                                                </td>
                                            </tr>
                                        <% }); %>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    <% } %>
                <% } %>
            </div>
        </div>
    </main>

    <script>
        // Define CSRF token globalmente
        window.csrfToken = '<%= typeof csrfToken !== "undefined" && csrfToken ? csrfToken : "" %>';

        // Máscara CNPJ
        document.querySelector('input[name="cnpjSindicato"]')?.addEventListener('input', function(e) {
            let value = e.target.value.replace(/\D/g, '');
            if (value.length <= 14) {
                value = value.replace(/^(\d{2})(\d{3})(\d{3})(\d{4})(\d{2}).*/, '$1.$2.$3/$4-$5');
            }
            e.target.value = value;
        });

        // Função para obter CSRF token
        function getCSRFToken() {
            // Tenta pegar do window.csrfToken (prioridade)
            if (typeof window !== 'undefined' && window.csrfToken) {
                return window.csrfToken;
            }
            
            // Tenta pegar do input hidden no form
            const csrfInput = document.querySelector('input[name="_csrf"]');
            if (csrfInput) return csrfInput.value;
            
            return '';
        }

        // Função para obter headers CSRF
        function getCSRFHeaders() {
            const token = getCSRFToken();
            return {
                'Content-Type': 'application/json',
                'X-CSRF-Token': token
            };
        }

        // Event listeners para botões de excluir
        document.addEventListener('DOMContentLoaded', function() {
            const deletarButtons = document.querySelectorAll('.deletar-consulta-btn');
            
            deletarButtons.forEach(button => {
                button.addEventListener('click', async function() {
                    const consultaId = this.getAttribute('data-id');
                    
                    if (!consultaId) {
                        alert('Erro: ID da consulta não encontrado.');
                        return;
                    }

                    // Confirmação
                    if (!confirm('Tem certeza que deseja excluir esta consulta? Esta ação não pode ser desfeita.')) {
                        return;
                    }

                    // Desabilita o botão durante a requisição
                    this.disabled = true;
                    this.innerHTML = '<i class="fas fa-spinner fa-spin"></i>';

                    try {
                        const response = await fetch(`/risco-multa/${consultaId}`, {
                            method: 'DELETE',
                            headers: getCSRFHeaders()
                        });

                        const data = await response.json();

                        if (data.success) {
                            // Remove a linha da tabela
                            const row = this.closest('tr');
                            row.style.transition = 'opacity 0.3s';
                            row.style.opacity = '0';
                            setTimeout(() => {
                                row.remove();
                                
                                // Verifica se não há mais consultas
                                const tbody = row.closest('tbody');
                                if (tbody && tbody.children.length === 0) {
                                    location.reload(); // Recarrega a página se não houver mais consultas
                                }
                            }, 300);
                        } else {
                            alert('Erro ao excluir consulta: ' + (data.message || 'Erro desconhecido'));
                            this.disabled = false;
                            this.innerHTML = '<i class="fas fa-trash"></i>';
                        }
                    } catch (error) {
                        console.error('Erro ao excluir consulta:', error);
                        alert('Erro ao excluir consulta. Tente novamente.');
                        this.disabled = false;
                        this.innerHTML = '<i class="fas fa-trash"></i>';
                    }
                });
            });
        });
    </script>
</body>
</html>
