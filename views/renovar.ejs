<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Renovar Assinatura - Suporte DP</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: {
                            red: '#DC2626',
                            yellow: '#FBBF24'
                        }
                    }
                }
            }
        }
    </script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>

<body class="bg-gray-50">
    <!-- Header -->
    <header class="bg-white shadow-md">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center">
                    <a href="/dashboard" class="flex items-center">
                        <i class="fas fa-calculator text-primary-red text-2xl mr-2"></i>
                        <span class="text-xl font-bold text-gray-900">Suporte DP</span>
                    </a>
                </div>
                <div class="flex items-center space-x-4">
                    <span class="text-sm text-gray-600">
                        <i class="fas fa-user mr-1"></i><%= user ? user.nome : 'Usuário' %>
                    </span>
                    <a href="/logout"
                        class="px-4 py-2 text-gray-700 hover:text-primary-red font-medium transition">
                        Sair
                    </a>
                </div>
            </div>
        </div>
    </header>

    <!-- Conteúdo Principal -->
    <main class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="bg-white rounded-lg shadow-xl p-8 md:p-12">
            <div class="text-center mb-8">
                <div class="bg-red-100 rounded-full w-20 h-20 flex items-center justify-center mx-auto mb-4">
                    <i class="fas fa-exclamation-triangle text-red-600 text-3xl"></i>
                </div>
                <h1 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
                    Renovar Assinatura
                </h1>
                <p class="text-lg text-gray-600">
                    Sua assinatura expirou. Renove para continuar usando o sistema.
                </p>
            </div>

            <% if (error) { %>
                <div class="bg-red-50 border-l-4 border-red-500 p-6 mb-8">
                    <div class="flex">
                        <div class="flex-shrink-0">
                            <i class="fas fa-exclamation-circle text-red-500 text-2xl"></i>
                        </div>
                        <div class="ml-3">
                            <h3 class="text-lg font-semibold text-red-900 mb-2">
                                Erro
                            </h3>
                            <p class="text-red-700">
                                <%= error %>
                            </p>
                        </div>
                    </div>
                </div>
            <% } %>

            <% if (user) { %>
                <div class="bg-yellow-50 border-l-4 border-yellow-500 p-6 mb-8">
                    <div class="flex">
                        <div class="flex-shrink-0">
                            <i class="fas fa-info-circle text-yellow-500 text-2xl"></i>
                        </div>
                        <div class="ml-3">
                            <h3 class="text-lg font-semibold text-yellow-900 mb-2">
                                Informações da Assinatura
                            </h3>
                            <div class="text-yellow-800 space-y-1">
                                <p><strong>Usuário:</strong> <%= user.nome %></p>
                                <p><strong>Email:</strong> <%= user.email %></p>
                                <% if (user.subscription_expires_at) { %>
                                    <p><strong>Expirou em:</strong> <%= new Date(user.subscription_expires_at).toLocaleDateString('pt-BR') %></p>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            <% } %>

            <!-- Informações de Preço -->
            <div class="bg-blue-50 border-l-4 border-blue-500 p-6 mb-8">
                <div class="flex">
                    <div class="flex-shrink-0">
                        <i class="fas fa-info-circle text-blue-500 text-2xl"></i>
                    </div>
                    <div class="ml-3">
                        <h3 class="text-lg font-semibold text-blue-900 mb-2">
                            Valor da Renovação
                        </h3>
                        <p class="text-blue-800">
                            A renovação custa <strong class="text-2xl">R$ 19,90</strong> e garante mais <strong>30 dias</strong> de acesso ao sistema.
                        </p>
                    </div>
                </div>
            </div>

            <% if (typeof checkoutUrl !== 'undefined' && checkoutUrl) { %>
                <!-- Link de pagamento gerado - mostrar botão para ir ao pagamento -->
                <div class="mt-8 p-6 bg-green-50 border-2 border-green-200 rounded-lg">
                    <div class="text-center mb-6">
                        <i class="fas fa-check-circle text-green-600 text-4xl mb-4"></i>
                        <h3 class="text-xl font-bold text-green-800 mb-2">Link de Pagamento Gerado!</h3>
                        <p class="text-gray-700">Clique no botão abaixo para ser redirecionado ao pagamento:</p>
                    </div>
                    <div class="flex flex-col sm:flex-row gap-4 justify-center">
                        <a href="/logout"
                            class="px-6 py-3 bg-gray-200 text-gray-800 rounded-md font-semibold hover:bg-gray-300 transition text-center">
                            <i class="fas fa-sign-out-alt mr-2"></i>Sair
                        </a>
                        <a href="<%= checkoutUrl %>"
                            class="px-8 py-3 bg-gradient-to-r from-primary-red to-primary-yellow text-white rounded-md font-semibold hover:opacity-90 transition shadow-lg text-center">
                            <i class="fas fa-credit-card mr-2"></i>Ir para Pagamento
                        </a>
                    </div>
                    <div class="mt-4 text-center">
                        <p class="text-sm text-gray-600">Pedido: <code class="bg-gray-100 px-2 py-1 rounded"><%= orderNsu %></code></p>
                        <p class="text-xs text-gray-500 mt-2">
                            <i class="fas fa-info-circle mr-1"></i>Após o pagamento, você será redirecionado para fazer login novamente.
                        </p>
                    </div>
                </div>
            <% } else { %>
                <!-- Formulário para gerar link de pagamento -->
                <form method="POST" action="/renovar" class="mt-8">
                    <div class="text-center">
                        <button type="submit"
                            class="px-8 py-4 bg-gradient-to-r from-primary-red to-primary-yellow text-white rounded-md font-semibold text-lg hover:opacity-90 transition shadow-lg">
                            <i class="fas fa-credit-card mr-2"></i>Renovar Assinatura - R$ 19,90
                        </button>
                    </div>
                    <p class="text-center text-sm text-gray-500 mt-4">
                        <i class="fas fa-shield-alt mr-1"></i>Pagamento seguro processado pela InfinitePay
                    </p>
                </form>
            <% } %>

            <!-- Informações Adicionais -->
            <div class="mt-8 pt-8 border-t border-gray-200">
                <h3 class="text-lg font-semibold text-gray-900 mb-4">
                    <i class="fas fa-question-circle text-primary-yellow mr-2"></i>Perguntas Frequentes
                </h3>
                <div class="space-y-4">
                    <div>
                        <h4 class="font-semibold text-gray-800">Após pagar, quando minha assinatura será renovada?</h4>
                        <p class="text-gray-600 text-sm mt-1">
                            Sua assinatura será renovada automaticamente assim que o pagamento for confirmado. Você receberá mais 30 dias de acesso a partir da data do pagamento.
                        </p>
                    </div>
                    <div>
                        <h4 class="font-semibold text-gray-800">Como faço login após renovar?</h4>
                        <p class="text-gray-600 text-sm mt-1">
                            Após o pagamento, você será redirecionado para a página de login. Use suas credenciais normalmente para acessar o sistema.
                        </p>
                    </div>
                    <div>
                        <h4 class="font-semibold text-gray-800">Preciso renovar todo mês?</h4>
                        <p class="text-gray-600 text-sm mt-1">
                            Sim, a assinatura é mensal. A cada 30 dias você precisa renovar para continuar usando o sistema. Você receberá acesso imediato após cada pagamento.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </main>
</body>

</html>



