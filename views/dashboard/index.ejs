<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: { red: '#DC2626', yellow: '#FBBF24' }
                    },
                    animation: {
                        'float': 'float 6s ease-in-out infinite',
                        'pulse-slow': 'pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite',
                        'gradient': 'gradient 15s ease infinite',
                        'slide-up': 'slideUp 0.5s ease-out',
                        'fade-in': 'fadeIn 0.6s ease-out',
                    },
                    keyframes: {
                        float: {
                            '0%, 100%': { transform: 'translateY(0px)' },
                            '50%': { transform: 'translateY(-20px)' }
                        },
                        gradient: {
                            '0%, 100%': { backgroundPosition: '0% 50%' },
                            '50%': { backgroundPosition: '100% 50%' }
                        },
                        slideUp: {
                            '0%': { transform: 'translateY(30px)', opacity: '0' },
                            '100%': { transform: 'translateY(0)', opacity: '1' }
                        },
                        fadeIn: {
                            '0%': { opacity: '0' },
                            '100%': { opacity: '1' }
                        }
                    }
                }
            }
        }
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/loading.css">
    <!-- Favicon com as letras "DP" nas cores do sistema (Vermelho #DC2626 e Amarelo #FBBF24) -->
    <link rel="icon" type="image/svg+xml"
        href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 200 200'%3E%3Cdefs%3E%3ClinearGradient id='grad' x1='0%25' y1='0%25' x2='100%25' y2='100%25'%3E%3Cstop offset='0%25' style='stop-color:%23DC2626;stop-opacity:1' /%3E%3Cstop offset='100%25' style='stop-color:%23FBBF24;stop-opacity:1' /%3E%3C/linearGradient%3E%3C/defs%3E%3Crect width='200' height='200' rx='20' fill='url(%23grad)'/%3E%3Ctext x='100' y='140' font-family='Arial, sans-serif' font-size='120' font-weight='bold' text-anchor='middle' fill='white'%3EDP%3C/text%3E%3C/svg%3E" />
    <link rel="apple-touch-icon"
        href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 200 200'%3E%3Cdefs%3E%3ClinearGradient id='grad' x1='0%25' y1='0%25' x2='100%25' y2='100%25'%3E%3Cstop offset='0%25' style='stop-color:%23DC2626;stop-opacity:1' /%3E%3Cstop offset='100%25' style='stop-color:%23FBBF24;stop-opacity:1' /%3E%3C/linearGradient%3E%3C/defs%3E%3Crect width='200' height='200' rx='20' fill='url(%23grad)'/%3E%3Ctext x='100' y='140' font-family='Arial, sans-serif' font-size='120' font-weight='bold' text-anchor='middle' fill='white'%3EDP%3C/text%3E%3C/svg%3E" />
    <style>
        * {
            font-family: 'Inter', sans-serif;
        }

        .glass {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .glass-dark {
            background: rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
        }

        .gradient-text {
            background: linear-gradient(135deg, #DC2626 0%, #FBBF24 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .gradient-bg {
            background: linear-gradient(135deg, #DC2626 0%, #FBBF24 50%, #DC2626 100%);
            background-size: 200% 200%;
            animation: gradient 15s ease infinite;
        }

        .pattern-dots {
            background-image: radial-gradient(circle, rgba(220, 38, 38, 0.1) 1px, transparent 1px);
            background-size: 20px 20px;
        }

        .pattern-grid {
            background-image:
                linear-gradient(rgba(220, 38, 38, 0.05) 1px, transparent 1px),
                linear-gradient(90deg, rgba(220, 38, 38, 0.05) 1px, transparent 1px);
            background-size: 30px 30px;
        }

        .icon-glow {
            filter: drop-shadow(0 0 10px rgba(220, 38, 38, 0.5));
        }

        .icon-glow-yellow {
            filter: drop-shadow(0 0 10px rgba(251, 191, 36, 0.5));
        }

        .input-glow:focus {
            box-shadow: 0 0 0 3px rgba(220, 38, 38, 0.1), 0 0 20px rgba(220, 38, 38, 0.2);
        }

        .card-hover {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .card-hover:hover {
            transform: translateY(-4px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }

        .border-3 {
            border-width: 3px !important;
        }
    </style>
</head>

<body class="bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 min-h-screen">
    <%- include('../partials/navbar') %>

    <!-- Loading Overlay -->
    <div id="loadingOverlay" class="loading-overlay">
        <div class="spinner"></div>
    </div>

    <main class="max-w-7xl mx-auto py-4 sm:px-6 lg:px-8 relative">
        <!-- Background Elements -->
        <div class="absolute top-0 right-0 w-96 h-96 bg-primary-red/10 rounded-full blur-3xl"></div>
        <div class="absolute bottom-0 left-0 w-96 h-96 bg-primary-yellow/10 rounded-full blur-3xl"></div>

        <div class="relative z-10">
            <!-- Header -->
            <div class="mb-4 animate-fade-in">
                <div class="flex items-center space-x-3">
                    <div class="relative">
                        <div
                            class="absolute inset-0 bg-gradient-to-r from-primary-red to-primary-yellow rounded-xl blur-lg opacity-50">
                        </div>
                        <div class="relative bg-gradient-to-r from-primary-red to-primary-yellow p-2 rounded-xl">
                            <i class="fas fa-home text-white text-xl icon-glow"></i>
                        </div>
                    </div>
                    <div>
                        <h1 class="text-lg sm:text-xl font-semibold text-white/90 tracking-wide">
                            Bem-vindo, <span class="gradient-text font-medium"><%= typeof user !=='undefined' && user ? user.nome : 'Usuário' %></span>
                        </h1>
                    </div>
                </div>
            </div>

            <div class="grid grid-cols-1 gap-4">
                <!-- Calendário -->
                <div class="glass rounded-2xl shadow-2xl p-4 sm:p-6 border border-white/20 backdrop-blur-xl card-hover animate-slide-up">
                    <div class="relative grid grid-cols-1 sm:grid-cols-3 items-center mb-4 gap-3">
                        <!-- Botões Anterior/Próximo à esquerda -->
                        <div class="flex space-x-2 justify-start">
                            <a href="?ano=<%= mes === 1 ? ano - 1 : ano %>&mes=<%= mes === 1 ? 12 : mes - 1 %>"
                                class="group relative bg-glass border border-white/20 text-white px-4 py-2 rounded-xl font-bold hover:shadow-lg hover:shadow-primary-red/50 transition-all transform hover:scale-105 overflow-hidden">
                                <span class="relative z-10 flex items-center">
                                    <i class="fas fa-chevron-left mr-2"></i>
                                    <span class="hidden sm:inline">Anterior</span>
                                </span>
                                <div
                                    class="absolute inset-0 bg-gradient-to-r from-primary-red to-primary-yellow opacity-0 group-hover:opacity-100 transition-opacity">
                                </div>
                            </a>
                            <a href="?ano=<%= mes === 12 ? ano + 1 : ano %>&mes=<%= mes === 12 ? 1 : mes + 1 %>"
                                class="group relative bg-glass border border-white/20 text-white px-4 py-2 rounded-xl font-bold hover:shadow-lg hover:shadow-primary-red/50 transition-all transform hover:scale-105 overflow-hidden">
                                <span class="relative z-10 flex items-center">
                                    <span class="hidden sm:inline">Próximo</span>
                                    <i class="fas fa-chevron-right ml-2"></i>
                                </span>
                                <div
                                    class="absolute inset-0 bg-gradient-to-r from-primary-yellow to-primary-red opacity-0 group-hover:opacity-100 transition-opacity">
                                </div>
                            </a>
                        </div>
                        
                        <!-- Título centralizado -->
                        <div class="flex items-center justify-center space-x-3">
                            <div class="relative">
                                <div
                                    class="absolute inset-0 bg-gradient-to-r from-primary-red to-primary-yellow rounded-xl blur opacity-50">
                                </div>
                                <div class="relative bg-gradient-to-r from-primary-red to-primary-yellow p-2 rounded-xl">
                                    <i class="fas fa-calendar text-white text-xl icon-glow"></i>
                                </div>
                            </div>
                            <h2 class="text-2xl sm:text-3xl lg:text-4xl font-bold text-white capitalize text-center">
                                <span class="gradient-text"><%= mesNome %></span> <%= ano %>
                            </h2>
                        </div>
                        
                        <!-- Botão Hoje à direita -->
                        <div class="flex space-x-2 justify-end">
                            <a href="?ano=<%= new Date().getFullYear() %>&mes=<%= new Date().getMonth() + 1 %>"
                                class="group relative bg-gradient-to-r from-primary-red to-primary-yellow text-white px-4 py-2 rounded-xl font-bold hover:shadow-2xl hover:shadow-primary-yellow/50 transition-all transform hover:scale-105 overflow-hidden">
                                <span class="relative z-10 flex items-center">
                                    <i class="fas fa-calendar-day mr-2"></i>
                                    Hoje
                                </span>
                                <div
                                    class="absolute inset-0 bg-gradient-to-r from-primary-yellow to-primary-red opacity-0 group-hover:opacity-100 transition-opacity">
                                </div>
                            </a>
                        </div>
                    </div>

                    <!-- Cabeçalho dos dias da semana -->
                    <div class="grid grid-cols-7 gap-1.5 mb-1.5">
                        <div
                            class="text-center font-bold text-white py-2 bg-gradient-to-br from-red-500 to-red-600 rounded-lg shadow-md text-xs sm:text-sm">
                            Dom</div>
                        <div
                            class="text-center font-bold text-white py-2 bg-gradient-to-br from-gray-600 to-gray-700 rounded-lg shadow-md text-xs sm:text-sm">
                            Seg</div>
                        <div
                            class="text-center font-bold text-white py-2 bg-gradient-to-br from-gray-600 to-gray-700 rounded-lg shadow-md text-xs sm:text-sm">
                            Ter</div>
                        <div
                            class="text-center font-bold text-white py-2 bg-gradient-to-br from-gray-600 to-gray-700 rounded-lg shadow-md text-xs sm:text-sm">
                            Qua</div>
                        <div
                            class="text-center font-bold text-white py-2 bg-gradient-to-br from-gray-600 to-gray-700 rounded-lg shadow-md text-xs sm:text-sm">
                            Qui</div>
                        <div
                            class="text-center font-bold text-white py-2 bg-gradient-to-br from-gray-600 to-gray-700 rounded-lg shadow-md text-xs sm:text-sm">
                            Sex</div>
                        <div
                            class="text-center font-bold text-white py-2 bg-gradient-to-br from-yellow-500 to-yellow-600 rounded-lg shadow-md text-xs sm:text-sm">
                            Sáb</div>
                    </div>

                    <!-- Dias do mês -->
                    <div class="grid grid-cols-7 gap-1.5 auto-rows-fr">
                        <% const primeiroDia=new Date(ano, mes - 1, 1).getDay(); for (let i=0; i <
                            primeiroDia; i++) { %>
                            <div class="min-h-[calc((100vh-350px)/6)]"></div>
                            <% } %>

                                <% if (typeof calendario !=='undefined' && calendario && calendario.length>
                                    0) { %>
                                    <% calendario.forEach(dia=> { %>
                                        <div onclick="abrirModal('<%= dia.data %>')"
                                            class="glass min-h-[calc((100vh-350px)/6)] flex flex-col border-3 rounded-xl p-1.5 sm:p-2 cursor-pointer transition-all duration-300 transform hover:scale-105 hover:shadow-xl hover:z-10 card-hover
                                    <%= dia.isFeriado && dia.feriadoTipo === 'nacional' ? 'border-red-500/50 hover:border-red-400' : 
                                        dia.isFeriado && dia.feriadoTipo === 'facultativo' ? 'border-orange-500/50 hover:border-orange-400' :
                                        dia.diaSemana === 0 ? 'border-gray-500/50 hover:border-gray-400' :
                                        dia.anotacao || (dia.obrigacoes && dia.obrigacoes.length > 0) || (dia.tarefas && dia.tarefas.length > 0) ? 'border-yellow-500/50 hover:border-yellow-400' : 
                                        'border-white/20 hover:border-primary-red/50' %>">
                                            <div class="flex justify-between items-start mb-0.5 flex-shrink-0">
                                                <span
                                                    class="text-sm sm:text-base font-bold <%= dia.isFeriado && dia.feriadoTipo === 'nacional' ? 'text-red-300' : dia.isFeriado && dia.feriadoTipo === 'facultativo' ? 'text-orange-300' : dia.diaSemana === 0 ? 'text-gray-400' : 'text-white' %>">
                                                    <%= typeof dia.dia !=='undefined' ? dia.dia : '' %>
                                                </span>
                                                <% const totalEventos=(dia.obrigacoes ?
                                                    dia.obrigacoes.length : 0) + (dia.tarefas ?
                                                    dia.tarefas.length : 0); %>
                                                    <% if (totalEventos> 0) { %>
                                                        <span
                                                            class="text-xs bg-gradient-to-r from-primary-yellow to-primary-red text-white px-2 py-0.5 rounded-full font-bold shadow-md transform hover:scale-110 transition-transform">
                                                            <%= totalEventos %>
                                                        </span>
                                                        <% } %>
                                            </div>

                                            <% if (dia.isFeriado) { %>
                                                <div class="text-xs <%= dia.feriadoTipo === 'facultativo' ? 'text-orange-300' : 'text-red-300' %> font-bold truncate"
                                                    title="<%= dia.feriadoNome %> (<%= dia.feriadoTipo === 'facultativo' ? 'Ponto Facultativo' : 'Feriado Nacional' %>)">
                                                    <i
                                                        class="fas fa-<%= dia.feriadoTipo === 'facultativo' ? 'calendar-check' : 'calendar-times' %> mr-1"></i>
                                                    <%= dia.feriadoNome %>
                                                        <% if (dia.feriadoTipo==='facultativo' ) { %>
                                                            <span
                                                                class="text-xs opacity-75">(Facultativo)</span>
                                                            <% } %>
                                                </div>
                                                <% } %>

                                                    <% if (dia.obrigacoes && dia.obrigacoes.length> 0) { %>
                                                        <div class="space-y-0.5 mt-0.5 flex-1 overflow-hidden">
                                                            <% dia.obrigacoes.slice(0,
                                                                2).forEach(obrigacao=> { %>
                                                                <div class="text-xs <%= obrigacao.tipo === 'fgts' ? 'text-orange-300 bg-orange-900/30 border border-orange-500/30' : obrigacao.tipo === 'inss' ? 'text-green-300 bg-green-900/30 border border-green-500/30' : obrigacao.tipo === 'irrf' ? 'text-purple-300 bg-purple-900/30 border border-purple-500/30' : obrigacao.tipo === 'dctfweb' ? 'text-blue-300 bg-blue-900/30 border border-blue-500/30' : obrigacao.tipo === 'efd_reinf' ? 'text-indigo-300 bg-indigo-900/30 border border-indigo-500/30' : 'text-gray-300 bg-gray-900/30 border border-gray-500/30' %> px-1.5 py-0.5 rounded-md truncate font-medium"
                                                                    title="<%= obrigacao.descricao %>">
                                                                    <i
                                                                        class="fas fa-<%= obrigacao.tipo === 'fgts' ? 'wallet' : obrigacao.tipo === 'inss' ? 'shield-alt' : obrigacao.tipo === 'irrf' ? 'receipt' : obrigacao.tipo === 'dctfweb' ? 'file-invoice' : obrigacao.tipo === 'efd_reinf' ? 'file-alt' : 'calendar-check' %> mr-1"></i>
                                                                    <span class="hidden sm:inline">
                                                                        <%= obrigacao.descricao %>
                                                                    </span>
                                                                </div>
                                                                <% }); %>
                                                                    <% if (dia.obrigacoes.length> 2) { %>
                                                                        <div
                                                                            class="text-xs text-white/60 font-semibold">
                                                                            +<%= dia.obrigacoes.length - 2
                                                                                %> mais
                                                                        </div>
                                                                        <% } %>
                                                        </div>
                                                        <% } %>

                                                            <% if (dia.tarefas && dia.tarefas.length> 0) {
                                                                %>
                                                                <div class="space-y-0.5 mt-0.5 flex-1 overflow-hidden">
                                                                    <% dia.tarefas.slice(0,
                                                                        2).forEach(tarefa=> { %>
                                                                        <% const
                                                                            corTipo=tarefa.tipo==='FÉRIAS'
                                                                            ? 'text-blue-300 bg-blue-900/30 border border-blue-500/30 font-semibold'
                                                                            :
                                                                            tarefa.tipo==='13° ADIANTAMENTO'
                                                                            || tarefa.tipo==='13° INTEGRAL'
                                                                            ? 'text-orange-300 bg-orange-900/30 border border-orange-500/30 font-semibold'
                                                                            : tarefa.tipo==='RESCISÃO'
                                                                            ? 'text-red-300 bg-red-900/30 border border-red-500/30 font-semibold'
                                                                            : tarefa.tipo==='ADMISSÃO'
                                                                            ? 'text-green-300 bg-green-900/30 border border-green-500/30 font-semibold'
                                                                            : tarefa.tipo==='AFASTAMENTO'
                                                                            ? 'text-yellow-300 bg-yellow-900/30 border border-yellow-500/30 font-semibold'
                                                                            :
                                                                            tarefa.tipo==='ALTERAÇÃO SALARIAL'
                                                                            ? 'text-purple-300 bg-purple-900/30 border border-purple-500/30 font-semibold'
                                                                            :
                                                                            tarefa.tipo==='ALTERAÇÃO DE CARGO'
                                                                            ? 'text-indigo-300 bg-indigo-900/30 border border-indigo-500/30 font-semibold'
                                                                            : tarefa.prioridade==='alta'
                                                                            ? 'text-red-300 bg-red-900/30 border border-red-500/30 font-semibold'
                                                                            : tarefa.prioridade==='media'
                                                                            ? 'text-yellow-300 bg-yellow-900/30 border border-yellow-500/30 font-semibold'
                                                                            : 'text-green-300 bg-green-900/30 border border-green-500/30 font-semibold'
                                                                            ; const
                                                                            concluida=tarefa.status==='feito'
                                                                            ; %>
                                                                            <a href="/tarefas?id=<%= tarefa.id %>"
                                                                                class="block text-xs <%= corTipo %> px-1.5 py-0.5 rounded-md truncate <%= concluida ? 'opacity-60 line-through' : '' %> hover:opacity-100 hover:shadow-md transition-all"
                                                                                title="<%= tarefa.nome %>">
                                                                                <i
                                                                                    class="fas fa-<%= concluida ? 'check-circle' : 'tasks' %> mr-1"></i>
                                                                                <%= tarefa.nome %>
                                                                            </a>
                                                                            <% }); %>
                                                                                <% if (dia.tarefas.length>
                                                                                    2) { %>
                                                                                    <div
                                                                                        class="text-xs text-white/60 font-semibold">
                                                                                        +<%= dia.tarefas.length
                                                                                            - 2 %> tarefas
                                                                                    </div>
                                                                                    <% } %>
                                                                </div>
                                                                <% } %>

                                                                    <% if (dia.anotacao) { %>
                                                                        <div class="text-xs text-yellow-300 mt-0.5 truncate bg-yellow-900/30 px-2 py-0.5 rounded-md border border-yellow-500/30 font-medium"
                                                                            title="<%= dia.anotacao %>">
                                                                            <i
                                                                                class="fas fa-sticky-note mr-1"></i><span
                                                                                class="hidden sm:inline">Anotação</span>
                                                                        </div>
                                                                        <% } %>
                                        </div>
                                        <% }); %>
                                            <% } else { %>
                                                <div class="col-span-7 text-center py-8 text-white/70">
                                                    <i class="fas fa-calendar-times text-4xl mb-2"></i>
                                                    <p>Carregando calendário...</p>
                                                </div>
                                                <% } %>
                    </div>
                </div>

                <!-- Acesso Rápido -->
                <div class="glass rounded-2xl shadow-2xl p-6 sm:p-8 border border-white/20 backdrop-blur-xl card-hover animate-slide-up"
                    style="animation-delay: 0.1s;">
                    <div class="flex items-center mb-6">
                        <div class="relative mr-3">
                            <div
                                class="absolute inset-0 bg-gradient-to-r from-primary-red to-primary-yellow rounded-xl blur opacity-50">
                            </div>
                            <div
                                class="relative bg-gradient-to-r from-primary-red to-primary-yellow p-2 rounded-xl">
                                <i class="fas fa-bolt text-white text-xl icon-glow"></i>
                            </div>
                        </div>
                        <h2 class="text-2xl font-black text-white">
                            Acesso <span class="gradient-text">Rápido</span>
                        </h2>
                    </div>
                    <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
                        <a href="/inss"
                            class="group glass border border-white/20 rounded-xl p-4 hover:border-primary-red/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-primary-red/30 card-hover">
                            <div class="flex items-center mb-2">
                                <i class="fas fa-percent text-primary-red text-2xl mr-3 icon-glow"></i>
                                <p class="font-bold text-white text-sm">INSS</p>
                            </div>
                            <p class="text-xs text-white/70 hidden sm:block">Cálculo progressivo</p>
                        </a>
                        <a href="/irrf"
                            class="group glass border border-white/20 rounded-xl p-4 hover:border-primary-yellow/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-primary-yellow/30 card-hover">
                            <div class="flex items-center mb-2">
                                <i class="fas fa-file-invoice-dollar text-primary-yellow text-2xl mr-3 icon-glow-yellow"></i>
                                <p class="font-bold text-white text-sm">IRRF</p>
                            </div>
                            <p class="text-xs text-white/70 hidden sm:block">Imposto de Renda</p>
                        </a>
                        <a href="/fgts"
                            class="group glass border border-white/20 rounded-xl p-4 hover:border-primary-red/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-primary-red/30 card-hover">
                            <div class="flex items-center mb-2">
                                <i class="fas fa-piggy-bank text-primary-red text-2xl mr-3 icon-glow"></i>
                                <p class="font-bold text-white text-sm">FGTS</p>
                            </div>
                            <p class="text-xs text-white/70 hidden sm:block">Fundo de Garantia</p>
                        </a>
                        <a href="/avos"
                            class="group glass border border-white/20 rounded-xl p-4 hover:border-primary-yellow/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-primary-yellow/30 card-hover">
                            <div class="flex items-center mb-2">
                                <i class="fas fa-gift text-primary-yellow text-2xl mr-3 icon-glow-yellow"></i>
                                <p class="font-bold text-white text-sm">13º Salário</p>
                            </div>
                            <p class="text-xs text-white/70 hidden sm:block">Cálculo proporcional</p>
                        </a>
                        <a href="/ferias"
                            class="group glass border border-white/20 rounded-xl p-4 hover:border-primary-yellow/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-primary-yellow/30 card-hover">
                            <div class="flex items-center mb-2">
                                <i class="fas fa-umbrella-beach text-primary-yellow text-2xl mr-3 icon-glow-yellow"></i>
                                <p class="font-bold text-white text-sm">Férias</p>
                            </div>
                            <p class="text-xs text-white/70 hidden sm:block">Cálculo proporcional</p>
                        </a>
                        <a href="/calendario"
                            class="group glass border border-white/20 rounded-xl p-4 hover:border-primary-red/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-primary-red/30 card-hover">
                            <div class="flex items-center mb-2">
                                <i class="fas fa-calendar text-primary-red text-2xl mr-3 icon-glow"></i>
                                <p class="font-bold text-white text-sm">Calendário</p>
                            </div>
                            <p class="text-xs text-white/70 hidden sm:block">Obrigações e feriados</p>
                        </a>
                        <a href="/custo"
                            class="group glass border border-white/20 rounded-xl p-4 hover:border-primary-yellow/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-primary-yellow/30 card-hover">
                            <div class="flex items-center mb-2">
                                <i class="fas fa-calculator text-primary-yellow text-2xl mr-3 icon-glow-yellow"></i>
                                <p class="font-bold text-white text-sm">Custo</p>
                            </div>
                            <p class="text-xs text-white/70 hidden sm:block">Simulador completo</p>
                        </a>
                        <a href="/periculosidade"
                            class="group glass border border-white/20 rounded-xl p-4 hover:border-primary-red/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-primary-red/30 card-hover">
                            <div class="flex items-center mb-2">
                                <i class="fas fa-exclamation-triangle text-primary-red text-2xl mr-3 icon-glow"></i>
                                <p class="font-bold text-white text-sm">Periculosidade</p>
                            </div>
                            <p class="text-xs text-white/70 hidden sm:block">Adicionais de risco</p>
                        </a>
                        <a href="/risco-multa"
                            class="group glass border border-white/20 rounded-xl p-4 hover:border-primary-yellow/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-primary-yellow/30 card-hover">
                            <div class="flex items-center mb-2">
                                <i class="fas fa-gavel text-primary-yellow text-2xl mr-3 icon-glow-yellow"></i>
                                <p class="font-bold text-white text-sm">Multa Data Base</p>
                            </div>
                            <p class="text-xs text-white/70 hidden sm:block">Período de risco</p>
                        </a>
                        <a href="/contrato-experiencia"
                            class="group glass border border-white/20 rounded-xl p-4 hover:border-primary-red/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-primary-red/30 card-hover">
                            <div class="flex items-center mb-2">
                                <i class="fas fa-file-contract text-primary-red text-2xl mr-3 icon-glow"></i>
                                <p class="font-bold text-white text-sm">Contrato Experiência</p>
                            </div>
                            <p class="text-xs text-white/70 hidden sm:block">Cálculo de multa</p>
                        </a>
                        <a href="/checklist"
                            class="group glass border border-white/20 rounded-xl p-4 hover:border-primary-yellow/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-primary-yellow/30 card-hover">
                            <div class="flex items-center mb-2">
                                <i class="fas fa-check-square text-primary-yellow text-2xl mr-3 icon-glow-yellow"></i>
                                <p class="font-bold text-white text-sm">Checklists</p>
                            </div>
                            <p class="text-xs text-white/70 hidden sm:block">Processos e documentos</p>
                        </a>
                        <% if (typeof user !=='undefined' && user && user.is_admin) { %>
                            <a href="/admin"
                                class="group glass border border-white/20 rounded-xl p-4 hover:border-primary-red/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-primary-red/30 card-hover">
                                <div class="flex items-center mb-2">
                                    <i class="fas fa-user-shield text-primary-red text-2xl mr-3 icon-glow"></i>
                                    <p class="font-bold text-white text-sm">Admin</p>
                                </div>
                                <p class="text-xs text-white/70 hidden sm:block">Painel administrativo</p>
                            </a>
                            <% } %>
                    </div>
                </div>

                <!-- Bloco de Contato -->
                <div class="glass rounded-2xl shadow-2xl p-6 sm:p-8 border border-white/20 backdrop-blur-xl card-hover animate-slide-up"
                    style="animation-delay: 0.2s;">
                    <div class="flex items-center mb-6">
                        <div class="relative mr-3">
                            <div
                                class="absolute inset-0 bg-gradient-to-r from-primary-yellow to-primary-red rounded-xl blur opacity-50">
                            </div>
                            <div
                                class="relative bg-gradient-to-r from-primary-yellow to-primary-red p-2 rounded-xl">
                                <i class="fas fa-headset text-white text-xl icon-glow-yellow"></i>
                            </div>
                        </div>
                        <h2 class="text-2xl font-black text-white">
                            Precisa de <span class="gradient-text">Ajuda?</span>
                        </h2>
                    </div>
                    <p class="text-sm text-white/70 mb-6 bg-white/5 p-4 rounded-lg border border-white/10">
                        <i class="fas fa-info-circle text-primary-yellow mr-2"></i>
                        Entre em contato com nosso suporte pelos canais abaixo.
                    </p>
                    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
                        <!-- Email -->
                        <a href="mailto:suporte@suportedp.com"
                            class="group glass border border-white/20 rounded-xl p-4 hover:border-primary-red/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-primary-red/30 text-center card-hover">
                            <div
                                class="w-12 h-12 bg-gradient-to-br from-primary-red/20 to-primary-red/30 rounded-xl flex items-center justify-center mb-3 mx-auto group-hover:from-primary-red/40 group-hover:to-primary-red/50 transition-colors">
                                <i class="fas fa-envelope text-primary-red text-xl icon-glow"></i>
                            </div>
                            <p class="font-bold text-white text-sm group-hover:text-primary-red transition-colors mb-1">
                                Email</p>
                            <p class="text-xs text-white/70">suporte@suportedp.com</p>
                        </a>

                        <!-- WhatsApp -->
                        <a href="https://wa.me/5511999999999?text=Olá,%20preciso%20de%20ajuda%20com%20o%20Suporte%20DP"
                            target="_blank" rel="noopener noreferrer"
                            class="group glass border border-white/20 rounded-xl p-4 hover:border-green-500/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-green-500/30 text-center card-hover">
                            <div
                                class="w-12 h-12 bg-gradient-to-br from-green-500/20 to-green-600/30 rounded-xl flex items-center justify-center mb-3 mx-auto group-hover:from-green-500/40 group-hover:to-green-600/50 transition-colors">
                                <i class="fab fa-whatsapp text-green-400 text-xl"></i>
                            </div>
                            <p class="font-bold text-white text-sm group-hover:text-green-400 transition-colors mb-1">
                                WhatsApp</p>
                            <p class="text-xs text-white/70">(11) 99999-9999</p>
                        </a>

                        <!-- Telefone -->
                        <a href="tel:+5511999999999"
                            class="group glass border border-white/20 rounded-xl p-4 hover:border-primary-yellow/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-primary-yellow/30 text-center card-hover">
                            <div
                                class="w-12 h-12 bg-gradient-to-br from-primary-yellow/20 to-primary-yellow/30 rounded-xl flex items-center justify-center mb-3 mx-auto group-hover:from-primary-yellow/40 group-hover:to-primary-yellow/50 transition-colors">
                                <i class="fas fa-phone text-primary-yellow text-xl icon-glow-yellow"></i>
                            </div>
                            <p class="font-bold text-white text-sm group-hover:text-primary-yellow transition-colors mb-1">
                                Telefone</p>
                            <p class="text-xs text-white/70">(11) 99999-9999</p>
                        </a>

                        <!-- Instagram -->
                        <a href="https://instagram.com/suportedp" target="_blank" rel="noopener noreferrer"
                            class="group glass border border-white/20 rounded-xl p-4 hover:border-pink-500/50 transition-all transform hover:scale-105 hover:shadow-lg hover:shadow-pink-500/30 text-center card-hover">
                            <div
                                class="w-12 h-12 bg-gradient-to-br from-pink-500/20 to-pink-600/30 rounded-xl flex items-center justify-center mb-3 mx-auto group-hover:from-pink-500/40 group-hover:to-pink-600/50 transition-colors">
                                <i class="fab fa-instagram text-pink-400 text-xl"></i>
                            </div>
                            <p class="font-bold text-white text-sm group-hover:text-pink-400 transition-colors mb-1">
                                Instagram</p>
                            <p class="text-xs text-white/70">@suportedp</p>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- 
        MODAL DE ANOTAÇÃO E OBRIGAÇÕES
        ===============================
        REUTILIZAÇÃO: Este modal usa os mesmos endpoints do calendário principal.
    -->
    <div id="modalAnotacao"
        class="hidden fixed inset-0 bg-black/50 backdrop-blur-sm z-50 flex items-center justify-center p-4">
        <div
            class="glass-dark border border-white/20 rounded-2xl p-6 max-w-2xl w-full max-h-[90vh] overflow-y-auto shadow-2xl">
            <div class="flex justify-between items-center mb-4">
                <h3 class="text-xl font-black text-white">Data: <span id="modalData"
                        class="text-base text-white/80"></span></h3>
                <button onclick="fecharModal()" class="text-white/70 hover:text-white transition-colors">
                    <i class="fas fa-times text-2xl"></i>
                </button>
            </div>

            <!-- Abas -->
            <div class="border-b border-white/20 mb-4">
                <nav class="flex -mb-px">
                    <button type="button" onclick="mostrarAba('anotacao')" id="tab-anotacao"
                        class="flex-1 sm:flex-none px-4 py-2 border-b-2 border-primary-red text-primary-red font-bold text-sm sm:text-base">
                        <i class="fas fa-sticky-note mr-2"></i><span
                            class="hidden sm:inline">Anotação</span><span class="sm:hidden">Nota</span>
                    </button>
                    <button type="button" onclick="mostrarAba('obrigacoes')" id="tab-obrigacoes"
                        class="flex-1 sm:flex-none px-4 py-2 border-b-2 border-transparent text-white/70 hover:text-white text-sm sm:text-base">
                        <i class="fas fa-calendar-check mr-2"></i><span
                            class="hidden sm:inline">Obrigações</span><span class="sm:hidden">Obrig.</span>
                    </button>
                </nav>
            </div>

            <!-- Aba Anotação -->
            <div id="content-anotacao" class="tab-content">
                <textarea id="modalTexto"
                    class="w-full px-4 py-3 glass border border-white/20 rounded-xl mb-4 text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-primary-red focus:border-primary-red transition-all input-glow backdrop-blur-sm resize-none"
                    rows="4" placeholder="Digite sua anotação para esta data..."></textarea>
                <div class="flex flex-col sm:flex-row justify-end gap-2">
                    <button onclick="fecharModal()"
                        class="w-full sm:w-auto px-4 py-2 glass-dark border border-white/20 text-white rounded-xl hover:bg-white/10 text-sm sm:text-base font-bold transition-all">
                        Cancelar</button>
                    <button onclick="salvarAnotacao()"
                        class="group relative bg-gradient-to-r from-primary-red to-primary-yellow text-white px-4 py-2 rounded-xl hover:shadow-lg hover:shadow-primary-red/50 font-bold text-sm sm:text-base transition-all transform hover:scale-105 overflow-hidden">
                        <span class="relative z-10 flex items-center justify-center">
                            <i class="fas fa-save mr-2"></i>Salvar Anotação
                        </span>
                        <div
                            class="absolute inset-0 bg-gradient-to-r from-primary-yellow to-primary-red opacity-0 group-hover:opacity-100 transition-opacity">
                        </div>
                    </button>
                </div>
            </div>

            <!-- Aba Obrigações -->
            <div id="content-obrigacoes" class="tab-content hidden">
                <div class="mb-4 glass-dark border border-white/20 p-4 rounded-xl">
                    <h4 class="font-bold mb-3 text-white text-sm sm:text-base">Adicionar Nova Obrigação</h4>
                    <div class="space-y-3">
                        <div>
                            <label class="block text-xs sm:text-sm font-semibold mb-1 text-white/90">Tipo de
                                Obrigação</label>
                            <select id="tipoObrigacao"
                                class="w-full px-4 py-3 glass border border-white/20 rounded-xl text-white focus:outline-none focus:ring-2 focus:ring-primary-red focus:border-primary-red transition-all input-glow backdrop-blur-sm">
                                <option value="fgts">FGTS</option>
                                <option value="inss">INSS</option>
                                <option value="irrf">IRRF</option>
                                <option value="dctfweb">DCTFWeb</option>
                                <option value="efd_reinf">EFD-Reinf</option>
                                <option value="esocial">eSocial</option>
                                <option value="outro">Outro</option>
                            </select>
                        </div>
                        <div>
                            <label class="block text-xs sm:text-sm font-semibold mb-1 text-white/90">Descrição</label>
                            <input type="text" id="descricaoObrigacao" placeholder="Ex: Vencimento DCTFWeb"
                                class="w-full px-4 py-3 glass border border-white/20 rounded-xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-primary-red focus:border-primary-red transition-all input-glow backdrop-blur-sm">
                        </div>
                        <div>
                            <label class="block text-xs sm:text-sm font-semibold mb-1 text-white/90">Observação
                                (opcional)</label>
                            <textarea id="observacaoObrigacao" placeholder="Observações adicionais..."
                                class="w-full px-4 py-3 glass border border-white/20 rounded-xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-primary-red focus:border-primary-red transition-all input-glow backdrop-blur-sm resize-none"
                                rows="2"></textarea>
                        </div>
                        <button onclick="adicionarObrigacao()"
                            class="group relative w-full bg-gradient-to-r from-primary-yellow to-primary-red text-white py-3 rounded-xl hover:shadow-lg hover:shadow-primary-yellow/50 font-bold text-sm sm:text-base transition-all transform hover:scale-105 overflow-hidden">
                            <span class="relative z-10 flex items-center justify-center">
                                <i class="fas fa-plus mr-2"></i>Adicionar Obrigação
                            </span>
                            <div
                                class="absolute inset-0 bg-gradient-to-r from-primary-red to-primary-yellow opacity-0 group-hover:opacity-100 transition-opacity">
                            </div>
                        </button>
                    </div>
                </div>

                <div id="listaObrigacoes" class="space-y-2 mb-4">
                    <p class="text-sm text-white/70 text-center py-4">Carregando obrigações...
                    </p>
                </div>

                <div class="flex justify-end">
                    <button onclick="fecharModal()"
                        class="w-full sm:w-auto px-4 py-2 glass-dark border border-white/20 text-white rounded-xl hover:bg-white/10 text-sm sm:text-base font-bold transition-all">
                        Fechar</button>
                </div>
            </div>
        </div>
    </div>

    <!-- 
        JAVASCRIPT - CONSUMO DE ENDPOINTS
        ===================================
        REUTILIZAÇÃO: Este JavaScript consome os mesmos endpoints do calendário principal.
    -->
    <script>
        let dataSelecionada = '';
        let obrigacoesAtuais = [];

        // REUTILIZA: Usa endpoint /calendario/anotacao/:data do calendário principal
        function abrirModal(data) {
            dataSelecionada = data;
            const dataFormatada = new Date(data + 'T00:00:00').toLocaleDateString('pt-BR', {
                weekday: 'long',
                year: 'numeric',
                month: 'long',
                day: 'numeric'
            });
            document.getElementById('modalData').textContent = dataFormatada;
            document.getElementById('modalAnotacao').classList.remove('hidden');
            mostrarAba('anotacao');

            // REUTILIZA: Endpoint do calendário principal
            fetch('/calendario/anotacao/' + data)
                .then(r => r.json())
                .then(result => {
                    if (result.data && result.data.anotacao) {
                        document.getElementById('modalTexto').value = result.data.anotacao;
                    } else {
                        document.getElementById('modalTexto').value = '';
                    }
                })
                .catch(() => {
                    document.getElementById('modalTexto').value = '';
                });

            // REUTILIZA: Carrega obrigações usando endpoint do calendário principal
            carregarObrigacoes(data);
        }

        function fecharModal() {
            document.getElementById('modalAnotacao').classList.add('hidden');
            obrigacoesAtuais = [];
        }

        function mostrarAba(aba) {
            document.querySelectorAll('.tab-content').forEach(c => c.classList.add('hidden'));
            document.querySelectorAll('[id^="tab-"]').forEach(b => {
                b.classList.remove('border-primary-red', 'text-primary-red', 'font-bold');
                b.classList.add('border-transparent', 'text-white/70');
            });

            document.getElementById('content-' + aba).classList.remove('hidden');
            const btn = document.getElementById('tab-' + aba);
            btn.classList.add('border-primary-red', 'text-primary-red', 'font-bold');
            btn.classList.remove('border-transparent', 'text-white/70');
        }

        // REUTILIZA: Endpoint /calendario/obrigacoes do calendário principal
        function carregarObrigacoes(data) {
            fetch(`/calendario/obrigacoes?data=${data}`)
                .then(r => r.json())
                .then(result => {
                    obrigacoesAtuais = result.data || [];
                    atualizarListaObrigacoes();
                })
                .catch(() => {
                    obrigacoesAtuais = [];
                    atualizarListaObrigacoes();
                });
        }

        function atualizarListaObrigacoes() {
            const lista = document.getElementById('listaObrigacoes');
            if (obrigacoesAtuais.length === 0) {
                lista.innerHTML = '<p class="text-sm text-white/70 text-center py-4">Nenhuma obrigação cadastrada para esta data</p>';
                return;
            }

            lista.innerHTML = obrigacoesAtuais.map(obrigacao => {
                const cores = {
                    'fgts': 'bg-orange-900/30 text-orange-300 border-orange-500/30',
                    'inss': 'bg-green-900/30 text-green-300 border-green-500/30',
                    'irrf': 'bg-purple-900/30 text-purple-300 border-purple-500/30',
                    'dctfweb': 'bg-blue-900/30 text-blue-300 border-blue-500/30',
                    'efd_reinf': 'bg-indigo-900/30 text-indigo-300 border-indigo-500/30',
                    'esocial': 'bg-teal-900/30 text-teal-300 border-teal-500/30',
                    'outro': 'bg-gray-900/30 text-gray-300 border-gray-500/30'
                };
                const cor = cores[obrigacao.tipo] || cores['outro'];

                return `
                    <div class="glass-dark border rounded-xl p-3 ${cor}">
                        <div class="flex justify-between items-start">
                            <div class="flex-1">
                                <p class="font-bold text-sm">${obrigacao.descricao}</p>
                                <p class="text-xs opacity-75 mt-1">${obrigacao.tipo.toUpperCase()}</p>
                                ${obrigacao.observacao ? `<p class="text-xs mt-2 opacity-90">${obrigacao.observacao}</p>` : ''}
                            </div>
                            <button onclick="removerObrigacao('${obrigacao.id}')" 
                                    class="text-red-400 hover:text-red-300 ml-2 transition-colors">
                                <i class="fas fa-trash"></i>
                            </button>
                        </div>
                    </div>
                `;
            }).join('');
        }

        // REUTILIZA: Endpoint POST /calendario/obrigacao do calendário principal
        function adicionarObrigacao() {
            const tipo = document.getElementById('tipoObrigacao').value;
            const descricao = document.getElementById('descricaoObrigacao').value.trim();
            const observacao = document.getElementById('observacaoObrigacao').value.trim();

            if (!descricao) {
                alert('Digite uma descrição para a obrigação');
                return;
            }

            fetch('/calendario/obrigacao', {
                method: 'POST',
                headers: getCSRFHeaders(),
                body: JSON.stringify({
                    data: dataSelecionada,
                    tipo,
                    descricao,
                    observacao: observacao || null,
                    _csrf: window.csrfToken
                })
            })
                .then(r => r.json())
                .then(result => {
                    if (result.success) {
                        document.getElementById('descricaoObrigacao').value = '';
                        document.getElementById('observacaoObrigacao').value = '';
                        carregarObrigacoes(dataSelecionada);
                        setTimeout(() => location.reload(), 500);
                    } else {
                        alert('Erro ao adicionar obrigação: ' + (result.error || 'Erro desconhecido'));
                    }
                })
                .catch(error => {
                    console.error('Erro:', error);
                    alert('Erro ao adicionar obrigação. Tente novamente.');
                });
        }

        // REUTILIZA: Endpoint DELETE /calendario/obrigacao/:id do calendário principal
        function removerObrigacao(id) {
            if (!confirm('Deseja remover esta obrigação?')) return;

            fetch(`/calendario/obrigacao/${id}`, {
                method: 'DELETE',
                headers: getCSRFHeaders(),
                body: JSON.stringify({ _csrf: window.csrfToken })
            })
                .then(r => r.json())
                .then(result => {
                    if (result.success) {
                        carregarObrigacoes(dataSelecionada);
                        setTimeout(() => location.reload(), 500);
                    } else {
                        alert('Erro ao remover obrigação');
                    }
                })
                .catch(error => {
                    console.error('Erro:', error);
                    alert('Erro ao remover obrigação. Tente novamente.');
                });
        }

        // REUTILIZA: Endpoint POST /calendario/anotacao do calendário principal
        function salvarAnotacao() {
            const texto = document.getElementById('modalTexto').value;
            fetch('/calendario/anotacao', {
                method: 'POST',
                headers: getCSRFHeaders(),
                body: JSON.stringify({ data: dataSelecionada, anotacao: texto, _csrf: window.csrfToken })
            })
                .then(r => r.json())
                .then(result => {
                    if (result.success) {
                        fecharModal();
                        location.reload();
                    } else {
                        alert('Erro ao salvar anotação');
                    }
                })
                .catch(error => {
                    console.error('Erro:', error);
                    alert('Erro ao salvar anotação. Tente novamente.');
                });
        }
    </script>
    <script src="/js/loading.js"></script>
</body>

</html>
