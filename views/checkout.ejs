<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Finalizar Pagamento - Suporte DP</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: {
                            red: '#DC2626',
                            yellow: '#FBBF24'
                        }
                    }
                }
            }
        }
    </script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>

<body class="bg-gray-50">
    <!-- Header -->
    <header class="bg-white shadow-md">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center">
                    <a href="/dashboard" class="flex items-center">
                        <i class="fas fa-calculator text-primary-red text-2xl mr-2"></i>
                        <span class="text-xl font-bold text-gray-900">Suporte DP</span>
                    </a>
                </div>
                <div class="flex items-center space-x-4">
                    <span class="text-sm text-gray-600">
                        <i class="fas fa-user mr-1"></i><%= user ? user.nome : 'Usuário' %>
                    </span>
                    <a href="/logout"
                        class="px-4 py-2 text-gray-700 hover:text-primary-red font-medium transition">
                        Sair
                    </a>
                </div>
            </div>
        </div>
    </header>

    <!-- Conteúdo Principal -->
    <main class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="bg-white rounded-lg shadow-xl p-8 md:p-12">
            <div class="text-center mb-8">
                <div class="bg-gradient-to-br from-primary-red to-primary-yellow rounded-full w-20 h-20 flex items-center justify-center mx-auto mb-4">
                    <i class="fas fa-credit-card text-white text-3xl"></i>
                </div>
                <h1 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
                    Finalizar Pagamento
                </h1>
                <p class="text-lg text-gray-600">
                    Complete seu pagamento para liberar o acesso ao sistema.
                </p>
            </div>

            <% if (error) { %>
                <div class="bg-red-50 border-l-4 border-red-500 p-6 mb-8">
                    <div class="flex">
                        <div class="flex-shrink-0">
                            <i class="fas fa-exclamation-circle text-red-500 text-2xl"></i>
                        </div>
                        <div class="ml-3">
                            <h3 class="text-lg font-semibold text-red-900 mb-2">
                                Erro
                            </h3>
                            <p class="text-red-700">
                                <%= error %>
                            </p>
                        </div>
                    </div>
                </div>
            <% } %>

            <% if (user) { %>
                <div class="bg-blue-50 border-l-4 border-blue-500 p-6 mb-8">
                    <div class="flex">
                        <div class="flex-shrink-0">
                            <i class="fas fa-info-circle text-blue-500 text-2xl"></i>
                        </div>
                        <div class="ml-3">
                            <h3 class="text-lg font-semibold text-blue-900 mb-2">
                                Informações da Conta
                            </h3>
                            <div class="text-blue-800 space-y-1">
                                <p><strong>Usuário:</strong> <%= user.nome %></p>
                                <p><strong>Email:</strong> <%= user.email %></p>
                            </div>
                        </div>
                    </div>
                </div>
            <% } %>

            <!-- Informações de Preço -->
            <div class="bg-green-50 border-l-4 border-green-500 p-6 mb-8">
                <div class="flex">
                    <div class="flex-shrink-0">
                        <i class="fas fa-tag text-green-500 text-2xl"></i>
                    </div>
                    <div class="ml-3">
                        <h3 class="text-lg font-semibold text-green-900 mb-2">
                            Valor da Assinatura
                        </h3>
                        <p class="text-green-800">
                            A assinatura custa <strong class="text-2xl">R$ 19,90</strong> e garante <strong>30 dias</strong> de acesso ao sistema.
                        </p>
                    </div>
                </div>
            </div>

            <% if (typeof checkoutUrl !== 'undefined' && checkoutUrl) { %>
                <!-- Link de pagamento gerado - mostrar botão para ir ao pagamento -->
                <div class="mt-8 p-6 bg-gradient-to-br from-green-50 to-blue-50 border-2 border-green-300 rounded-lg shadow-lg">
                    <div class="text-center mb-6">
                        <i class="fas fa-check-circle text-green-600 text-5xl mb-4"></i>
                        <h3 class="text-2xl font-bold text-green-800 mb-2">Link de Pagamento Gerado!</h3>
                        <p class="text-gray-700 text-lg">Clique no botão abaixo para ser redirecionado ao pagamento:</p>
                    </div>
                    <div class="flex flex-col sm:flex-row gap-4 justify-center">
                        <a href="/dashboard"
                            class="px-6 py-3 bg-gray-200 text-gray-800 rounded-md font-semibold hover:bg-gray-300 transition text-center">
                            <i class="fas fa-arrow-left mr-2"></i>Voltar
                        </a>
                        <a href="<%= checkoutUrl %>" target="_blank"
                            class="px-8 py-4 bg-gradient-to-r from-primary-red to-primary-yellow text-white rounded-md font-bold text-lg hover:opacity-90 transition shadow-xl text-center transform hover:scale-105">
                            <i class="fas fa-credit-card mr-2"></i>Ir para Pagamento
                        </a>
                    </div>
                    <div class="mt-6 text-center">
                        <p class="text-sm text-gray-600 mb-2">Pedido: <code class="bg-gray-100 px-2 py-1 rounded font-mono"><%= orderNsu %></code></p>
                        <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mt-4">
                            <p class="text-xs text-yellow-800">
                                <i class="fas fa-info-circle mr-1"></i>
                                <strong>Importante:</strong> Após o pagamento, o sistema será atualizado automaticamente e você terá acesso liberado por 30 dias. 
                                Você pode voltar ao sistema e verificar o status do pagamento.
                            </p>
                        </div>
                    </div>
                </div>
            <% } else { %>
                <!-- Formulário para gerar link de pagamento -->
                <form method="POST" action="/checkout" class="mt-8">
                    <div class="text-center">
                        <button type="submit"
                            class="px-12 py-4 bg-gradient-to-r from-primary-red to-primary-yellow text-white rounded-lg font-bold text-lg hover:opacity-90 transition shadow-xl transform hover:scale-105">
                            <i class="fas fa-credit-card mr-2"></i>Gerar Link de Pagamento
                        </button>
                    </div>
                    <div class="mt-6 text-center">
                        <p class="text-sm text-gray-500">
                            <i class="fas fa-info-circle mr-1"></i>
                            Clique no botão acima para gerar o link de pagamento seguro.
                        </p>
                    </div>
                </form>
            <% } %>
        </div>
    </main>
</body>

</html>

